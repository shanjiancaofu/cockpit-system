if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
    add_executable(camera-probe main.cc camera_probe.cc)
    target_include_directories(camera-probe PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
    target_link_libraries(camera-probe PRIVATE logging runtime v4l2_camera)
endif()
