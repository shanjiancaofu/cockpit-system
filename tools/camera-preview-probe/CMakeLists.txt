if(TARGET gstreamer_camera)
    add_executable(camera-preview-probe main.cc camera_preview_probe.cc)
    target_include_directories(camera-preview-probe PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
    target_link_libraries(camera-preview-probe PRIVATE gstreamer_camera camera_frames logging runtime)
    if(TARGET software_isp_camera_source)
        target_link_libraries(camera-preview-probe PRIVATE software_isp_camera_source)
    endif()
    if(TARGET software_isp_camera_source)
        target_compile_definitions(camera-preview-probe PRIVATE COCKPIT_CAMERA_HAS_SOFTWARE_ISP=1)
    endif()
endif()
