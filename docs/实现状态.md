# 实现状态

更新时间：2026-07-17。

本文只记录代码已经落地到什么程度、入口在哪里、如何验证、下一步补什么。
项目推进顺序见 [项目进度总览.md](项目进度总览.md)，架构解释见
[architecture.md](architecture.md)。

## 状态分级

| 状态 | 含义 |
|---|---|
| 已落地 | 已有代码、构建通过，并进入 smoke 或单元测试 |
| 可用原型 | 主链路可跑，但还缺真实硬件、真实性能或异常场景验证 |
| 部分完成 | 有基础模块或接口，主链路尚未闭环 |
| 待实现 | 只有规划或占位，没有可用代码 |

## 模块状态表

| 模块 | 当前状态 | 主要入口 | 验证方式 | 下一步 |
|---|---|---|---|---|
| 工程基础 | 已落地 | `CMakeLists.txt`、`scripts/`、`.pre-commit-config.yaml` | Clang Debug、GCC Release、CTest、smoke、WSL matrix、clang-tidy 和 pre-commit | 随业务改动处理相关非阻断静态告警 |
| 配置与日志 | 可用原型 | `cockpit/core/config`、`cockpit/core/logging` | 活动字段真实消费、严格未知字段、周期刷盘、时间切分、按模块保留上限和资源基线 | Jetson 长运行验证 |
| Navigator 动态运行时 | 可用原型 | `cockpit/navigator`、`cockpit/library` | mode、有界 IPC、重复崩溃恢复以及 RSS/线程/FD/目录占用基线 | Jetson systemd 与实机长稳验证 |
| MessageBus | 已落地 | `cockpit/core/event` | `message_bus_test` | 仅在出现真实低频事件 consumer 时接入，不扩成跨进程总线 |
| IPC shared memory | 已落地 | `cockpit/core/ipc` | camera shared memory 崩溃恢复、帧边界测试和 UI 联调 | Jetson 权限、进程并发启动和长稳验证 |
| 车辆与 CAN | 可用原型 | `library/driver/vehicle`、`drivers/socketcan`、`modules/vehicle` | `vcan0`、`run_vcan_smoke.sh`、Navigator smoke | 接正式 DBC 或车辆信号定义 |
| Transfer 与 topic | 已落地 | `library/transfer`、`tools/topic` | transport、更新周期、可用状态、错误原因、gRPC test、Navigator smoke | 接入真实新数据源时扩展 metadata |
| 音频采集 | 可用原型 | `library/driver/audio`、`modules/audio/capture`、`drivers/alsa` | audio tests、null/ALSA smoke、Navigator smoke | Jetson 麦克风、AEC、增益和阈值标定 |
| 语音识别 | 可用原型 | `modules/voice/asr`、`library/driver/audio` | mock ASR；可选 whisper.cpp adapter、真实 WAV 和独立 Release 基线 | Jetson 性能、功耗和真实麦克风验证 |
| 语音交互/Agent | 可用原型 | `library/agent`、`modules/voice/assistant` | 连续命令顺序、主动中断、队列丢弃、provider 超时/失败恢复和 voice smoke | 真实唤醒词、声学打断和 LLM provider deadline |
| HMI 动作桥接 | 部分完成 | `library/agent/hmi`、`apps/cockpit-ui/hmi_control.*` | `open_camera` Qt 主线程切页、媒体未接入失败路径、Navigator smoke | 媒体责任边界明确后接 Qt/Android 播放器 |
| 语音播放 | 可用原型 | `library/driver/audio`、`modules/voice/responses`、`drivers/alsa` | mock TTS 测试音、异步播放和有界停止测试 | 接真实 TTS provider 和扬声器标定 |
| 相机采集 | 可用原型 | `library/driver/camera`、`drivers/v4l2`、`modules/camera/capture` | 合成故障测试、共享内存恢复、Navigator smoke | Jetson CSI pipeline 和实机长稳测试 |
| UI 健康总览 | 可用原型 | `cockpit/core/health`、`cockpit/apps/cockpit-ui/health`、QML Dashboard/Diagnostics | health/model tests、Qt offscreen、smoke | Jetson 图形栈和长运行状态可见性验证 |
| 相机 UI | 可用原型 | `cockpit/apps/cockpit-ui/camera` | Qt offscreen、camera UI 脚本 | 补齐相机故障分类可视化 |
| 研发录包 | 可用原型 | `library/recording`、`modules/recording`、`recording-ctl` | recording tests、Navigator development mode、时间线、完整性和聚合报告 | 按长稳规模验证报告限制与生成耗时 |
| 诊断 CLI | 已落地 | `tools/diagnostics`、`cockpit-ctl`、`camera-ctl`、`voice-ctl`、`audio-probe` | `cli_output_test`、在线 JSON smoke、退出码检查 | 按实际自动化需求扩展其他命令 |
| 诊断快照 | 已落地 | `tools/cockpit-ctl/snapshot_command.*`、`scripts/run_navigator_stability.sh` | 离线/在线快照、失败自动留证、保留上限和长运行目录占用 | Jetson 故障现场验证 |
| 部署脚本 | 可用原型 | `scripts/package.sh`、`scripts/deploy`、`configs/systemd` | Release 双版本、checksum、临时安装、mode health 和 rollback 矩阵 | Jetson 实机部署和 systemd 长运行验证 |
| 云端上传 | 待实现 | `library/carupload` ABI 占位 | cloud mode 只验证模块生命周期，不建立 broker 连接 | 等 MQTT topic、设备身份、TLS、重试和后端协议明确后实现 |
| HMI 动态模块 | 可用原型 | `library/hmi`、`apps/cockpit-ui` | Qt offscreen、UI/HMI 双向崩溃恢复测试、Navigator smoke | Jetson 图形栈、开机显示和触摸实测 |
| Debugger/Calibration/Watchdog | 待实现 | 对应 `library/*` ABI 骨架 | 骨架拒绝启动 | 远程触发或持续采样需求明确后实现 debugger；其余等待硬件条件 |
| WebSocket/Web UI | 待实现 | 无 | 无 | 车端链路稳定后再决定是否做 |
| SQLite 状态索引 | 待实现 | 无 | 无 | 录包和状态查询需求明确后再加 |
| Upgrade/Safe OTA | 可用原型 | `library/upgrader`、`tools/safe-ota` | 事务 fsync、四阶段重启恢复、启动前自动恢复、健康检查和回滚 | 实机拔电、后端 PKI/CA 和车辆安全条件 |
| 崩溃收集 | 待实现 | 无 | 无 | 后期产品化阶段处理 |

## 当前验证基线

开发机环境：WSL2 Ubuntu 22.04，Clang 14 用于 Debug 开发检查，GCC 11.4 用于 Linux Release
正式构建；CMake 3.22、Ninja 1.10、Qt 6.2。

```bash
bash scripts/build.sh
bash scripts/build.sh --type release
bash scripts/run_smoke.sh
bash scripts/run_wsl_matrix.sh
pre-commit run --all-files
```

最近基线：x86_64 Debug 全量 45/45 CTest 和 Navigator 单入口完整 smoke 通过；语音测试覆盖连续命令顺序、
主动中断当前 action、丢弃排队 transcript、provider 超时和失败后继续服务。Navigator 测试覆盖有界 IPC、模式切换、
`cockpit-ctl runtime`、显式重启、整组 reload、真实 development 模块健康采样、camera driver 崩溃恢复、
HMI/UI 双向崩溃恢复、重启限制和 ABI 不兼容拒绝。safe-ota 测试覆盖候选确认、SHA256、独立安装、
真实 upgrade mode、prepared 切换前后、activated、confirmed 重启恢复、健康检查和失败回滚。WSL 矩阵
覆盖配置边界、Release 双版本、包篡改
拒绝、临时安装、配置保留、mode health、gateway 断流恢复、recording 不可达、队列满、shared-memory
writer 重启、离线诊断快照和 rollback，并输出 `_output/runtime/reports/wsl-matrix.json`。诊断快照同时在
在线 smoke 中验证 Runtime 状态，日志按文件数和字节数截断，且不复制可能包含凭据的完整配置文件。
当前 `clang-tidy` 的 WarningsAsErrors 阻断基线通过；其余第三方头文件和非阻断告警不在本批做无关清理。

WSL-R3 的 normal/development 30 秒基线各完成 3 次 camera driver 崩溃恢复，资源样本均无缺失。
normal RSS 增加 604 KiB、FD 从 89 降至 86；development RSS 增加 928 KiB、FD 从 109 降至 106。
两次运行的录包、报告和快照占用不增长，日志分别增加约 44 KiB 和 39 KiB。

WSL-R4 使用 whisper.cpp `6fc7c33b`、`ggml-small.bin` 和 16 kHz mono JFK WAV 完成独立 GCC Release
真实识别。输出文本正确，耗时 4.39 秒，CPU 393%，峰值 RSS 649232 KiB；模型仍在仓库外，默认
Clang Debug 和 GCC Release 构建保持 `BUILD_WHISPER_CPP_ASR=OFF`。

这条基线表示 WSL 原型具备自动验证证据，不表示真实 TTS、HMI 媒体控制、云端上传、Jetson 硬件或
量产升级已经完成。whisper.cpp 模型测试仅在显式配置模型时进入测试集。

## 明确未验证

- Jetson 实机部署、systemd 长运行和硬件权限。
- 真实车辆 CAN、GPIO、I2C 等外设。
- 真实麦克风、扬声器、AEC 和噪声环境。
- CSI 摄像头、Jetson GStreamer pipeline 和长时间预览稳定性。
- whisper.cpp Jetson CUDA/TensorRT 性能。
- MQTT/TLS/鉴权、量产 OTA、崩溃收集和云端平台。
