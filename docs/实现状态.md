# 实现状态

更新时间：2026-07-11。

本文只记录代码已经落地到什么程度、入口在哪里、如何验证、下一步补什么。
项目推进顺序见 [项目进度总览.md](项目进度总览.md)，架构解释见
[architecture.md](architecture.md)，长期蓝图见 [architecture_refined_v0.4.md](architecture_refined_v0.4.md)。

## 状态分级

| 状态 | 含义 |
|---|---|
| 已落地 | 已有代码、构建通过，并进入 smoke 或单元测试 |
| 可用原型 | 主链路可跑，但还缺真实硬件、真实性能或异常场景验证 |
| 部分完成 | 有基础模块或接口，主链路尚未闭环 |
| 待实现 | 只有规划或占位，没有可用代码 |

## 模块状态表

| 模块 | 当前状态 | 主要入口 | 验证方式 | 下一步 |
|---|---|---|---|---|
| 工程基础 | 已落地 | `CMakeLists.txt`、`scripts/`、`.pre-commit-config.yaml` | `bash scripts/build.sh`、`pre-commit run --all-files` | 继续按模块收口 clang-tidy 和 include 风格 |
| 配置与日志 | 已落地 | `cockpit/core/config`、`cockpit/core/logging` | `system_config_test`、smoke 启动日志 | 增加更细的配置错误提示 |
| Runtime 生命周期 | 已落地 | `cockpit/core/runtime` | runtime/module 相关测试、服务 smoke | 增加更完整的模块依赖可视化 |
| MessageBus | 已落地 | `cockpit/core/event` | `message_bus_test` | 扩展更多服务内小事件接入 |
| IPC shared memory | 已落地 | `cockpit/core/ipc` | camera shared memory 崩溃恢复、帧边界测试和 UI 联调 | Jetson 权限、服务并发启动和长稳验证 |
| 车辆与 CAN | 可用原型 | `cockpit/drivers/socketcan`、`cockpit/modules/vehicle`、`vehicle-data-service` | `vcan0`、`run_vcan_smoke.sh`、smoke | 接正式 DBC 或车辆信号定义 |
| Gateway 与 topic | 已落地 | `cockpit-gateway-service`、`tools/topic` | topic list/info/echo/hz、原生 health、smoke | 增加更多 topic 元数据和错误提示 |
| 音频采集 | 可用原型 | `audio-service`、`cockpit/modules/audio/capture`、`drivers/alsa` | audio tests、null/ALSA smoke | Jetson 麦克风、AEC、增益和阈值标定 |
| 语音识别 | 可用原型 | `cockpit/modules/voice/asr`、`voice-interaction-service` | mock ASR、whisper.cpp WSL CPU 测试 | Jetson CUDA/TensorRT 性能验证和真实模型选择 |
| 语音交互 | 可用原型 | `voice-interaction-service`、`cockpit/modules/voice/assistant` | voice smoke、取消和队列丢弃测试、voice-ctl | 唤醒词、用户打断、连续对话和 LLM provider |
| 语音播放 | 可用原型 | `audio-service`、`cockpit/modules/voice/responses`、`drivers/alsa` | mock TTS、异步播放和有界停止测试 | 接真实 TTS provider 和扬声器标定 |
| 相机采集 | 可用原型 | `camera-service`、`drivers/v4l2`、`modules/camera/capture` | USB `/dev/video0` 30 帧采集、camera probe、restart/recover 单测 | Jetson CSI pipeline、断连恢复和长稳测试 |
| UI 健康总览 | 可用原型 | `cockpit/apps/cockpit-ui/health`、QML Dashboard | Qt offscreen、camera UI 脚本、smoke | 收口 degraded、disabled 和 unknown 语义 |
| 相机 UI | 可用原型 | `cockpit/apps/cockpit-ui/camera` | Qt offscreen、camera UI 脚本 | 相机服务错误分类可视化 |
| 研发录包 | 可用原型 | `recording-service`、`cockpit/modules/recording`、`recording-ctl` | recording tests、smoke、会话内 artifact、完成标记恢复与多源时间线测试 | 增加录包文件完整性诊断 |
| 诊断 CLI | 已落地 | `cockpit-ctl`、`camera-ctl`、`voice-ctl`、`audio-probe` | CLI smoke、手动命令 | 统一输出格式和错误码 |
| 部署脚本 | 可用原型 | `scripts/package_release.sh`、`configs/systemd` | x86_64 build/package、脚本检查 | Jetson 实机部署和 systemd 长运行验证 |
| 云端上传 | 部分完成 | `cloud-uplink-service` | placeholder smoke | MQTT 客户端、TLS 和鉴权 |
| WebSocket/Web UI | 待实现 | 无 | 无 | 车端链路稳定后再决定是否做 |
| SQLite 状态索引 | 待实现 | 无 | 无 | 录包和状态查询需求明确后再加 |
| OTA/崩溃收集 | 待实现 | 无 | 无 | 后期产品化阶段处理 |

## 当前验证基线

开发机环境：WSL2 Ubuntu 22.04，GCC 11.4、CMake 3.22、Ninja 1.10、Qt 6.2。

```bash
bash scripts/build.sh
bash scripts/run_smoke.sh
pre-commit run --all-files
```

最近基线：x86_64 Debug 构建通过，CTest 31/31 通过，smoke 覆盖 VehicleState、recording、topic、audio、voice、camera 和 cloud placeholder。

## 明确未验证

- Jetson 实机部署、systemd 长运行和硬件权限。
- 真实车辆 CAN、GPIO、I2C 等外设。
- 真实麦克风、扬声器、AEC 和噪声环境。
- CSI 摄像头、Jetson GStreamer pipeline 和长时间预览稳定性。
- whisper.cpp Jetson CUDA/TensorRT 性能。
- MQTT/TLS/鉴权、OTA、崩溃收集和云端平台。
