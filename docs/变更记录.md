# 变更记录

本文记录 cockpit-system 的每批实现改动。后续记录统一包含变更内容、设计决定和验证结果。

## 2026-08-15 - Sherpa VM smoke 固化

- 新增 `scripts/tests/sherpa-voice-smoke.sh` 作为 Ubuntu 22.04 x86_64 VM 的固定 Sherpa 验证入口；
  脚本先检查 runtime/model/fixture，再构建 `COCKPIT_ENABLE_SHERPA_AGENT=ON` 的 Debug 目标并运行
  `sherpa_voice_smoke_test`。
- 更新 README、部署说明和语音 Agent 阶段任务，统一记录 VM Sherpa smoke 的运行命令和当前边界。
- 该入口不下载资源；缺少 `_output/ai` 资源时会明确提示先执行 `prepare-sherpa-runtime.sh` 与
  `prepare-voice-models.sh`。

## 2026-08-15 - Ubuntu VM Sherpa 真实 smoke 跑通

- 新增仅在 `COCKPIT_ENABLE_SHERPA_AGENT=ON` 时编译/注册的 `sherpa_voice_smoke_test`，默认
  Sherpa-OFF CI 不引入模型和 runtime 依赖。
- smoke 使用 `_output/ai/fixtures` 的固定 WAV 验证：`你好小车` KWS 命中、silence 不误唤醒、
  command-only 语音不误唤醒。
- 使用真实 Silero VAD、`SpeechSegmenter` 和 SenseVoiceSmall INT8 跑通 `open-camera-zh.wav`，
  将 transcript 交给 `TranscriptNormalizer + DeterministicCommandRouter`，最终路由到
  `OpenCamera`。
- SenseVoice 在当前样本上输出 `开相机`，确定性命令白名单补入 `开相机/开摄像头` 及礼貌前缀
  变体；仍保持完整 transcript 精确匹配，不恢复 substring 触发。
- 验证通过：`sherpa_voice_smoke_test`、`deterministic_command_router_test`。

## 2026-08-15 - Ubuntu VM Sherpa 资源准备完成

- 在 Ubuntu 22.04 x86_64 VM 上准备了 `_output/ai/runtime/sherpa-onnx/v1.13.4`，并补齐
  `include/sherpa-onnx/c-api/c-api.h` 与 `lib/libsherpa-onnx-c-api.so`。
- 下载并展开了 KWS、Silero VAD、SenseVoiceSmall INT8 模型到 `_output/ai/models/...`，
  并生成了 `_output/ai/config/kws-keywords.txt`。
- `COCKPIT_ENABLE_SHERPA_AGENT=ON` 的 x86_64 Debug 构建已通过，`agent_sherpa_kws` 也已更名为
  `agent_sherpa_voice`。
- 生成了最小本地 WAV 输入，后续继续补真实 KWS/VAD/ASR smoke。

## 2026-08-15 - AI 产品路径与模型准备修正

- `prepare-voice-models.sh` 支持 `.tar.bz2` / `.tbz2`，URL 下载保留真实 archive 文件名，避免
  固定 `.archive` 扩展导致无法解包官方 KWS / SenseVoice 模型。
- KWS 模型准备增加 `en.phone` 必需文件，并补全 `sherpa-onnx-cli text2token` 自动生成
  `kws-keywords.txt` 的流程；缺少工具时打印完整命令，不在 C++ runtime 中实现拼音/tokenizer。
- 生产配置、systemd 和安装脚本统一使用 `/cockpit-system/ai`；开发默认继续使用 `_output/ai`，
  runtime/model 不进 Git、不在程序启动时下载。
- 文档同步修正旧 `CommandMatcher`、旧 `/cockpit-system/models` 和旧 Agent/model current 路径；
  Stage 8 明确当前状态为 provider 代码已起步，真实 Sherpa 产品构建和 Jetson smoke 待执行。

## 2026-08-15 - Voice Agent KWS / AI provider 收口

- `VoiceInputGate` 的 wake cue 改为内部异步线程，PCM worker 不再被固定提示音阻塞；wake prompt
  失败不再误进入 `Listening`，`Stop()` 会先取消/停止再 join，避免析构时留下可 join 线程。
- Sherpa KWS provider 收紧为官方 chunk-8 模型布局，初始化前逐个检查缺失文件；`wake_word` 只供
  mock provider 使用，Sherpa provider 只接受 tokenized `keywords_file` 和模型目录。
- `cockpit/core/config` 同步收紧 `kws` / `vad` / `asr` provider 合同，增加 Sherpa Silero VAD 和
  Sherpa SenseVoice provider 的允许值；基础 CI 仍默认走 mock。
- 新增 `_output/ai` 本地资源布局说明和 `scripts/prepare-sherpa-runtime.sh`、
  `scripts/prepare-voice-models.sh` 准备脚本，约束 runtime/model 不进 Git、不在程序启动时下载。
- 新增 Sherpa Silero VAD 与 SenseVoiceSmall INT8 provider 代码骨架；普通构建继续不依赖 Sherpa
  runtime/model，Jetson smoke 仍待完成。
- ALSA capture XRUN 恢复在 `PREPARED` 状态补 `snd_pcm_start()`，防止恢复后 capture 仍停在准备态。

## 2026-08-15 - 文档中文命名收口

- 将 `docs/` 下常用文档统一改为中文文件名，目录中可直接看到“实现状态、项目进度总览、系统架构、
  配置说明、部署说明、语音Agent架构、语音Agent阶段任务”等职责。
- 更新根 `README.md`、文档导航、内部交叉引用和历史参考中的当前状态链接，避免重命名后出现死链。
- `文档导航.md` 继续作为入口，保留“按任务读最小文档集合”的规则；`reference/` 仍只作为历史参考。

## 2026-08-15 - Voice Agent Stage 7 KWS 主链路起步

- 新增 `agent/runtime/voice_input_gate`，把 PCM 输入按 `VoiceInteractionService` 状态分到
  KWS / Speech / Paused 三个模式；KWS 关闭时保持旧的 speech 直通行为。
- 新增最小 `WakeWordDetector` 接口、`MockWakeWordDetector`、`WakePromptPlayer` 和固定 PCM 提示音
  播放路径；唤醒词可通过 `features.voice.kws.wake_word` 或 `keywords_file` 配置，不再写死在代码里。
- `SpeechPipeline` 增加 `ResetInputState()`，用于 KWS 唤醒后清理上一次半句残留；`VoiceInteractionService`
  增加 wake 通知接口，继续由状态机统一管理会话转换。
- `features.voice.kws.provider=sherpa` 仅在显式 `COCKPIT_ENABLE_SHERPA_AGENT` 产品构建下启用，普通 CI
  不依赖 Sherpa/ONNX Runtime；`model_dir` 作为产品边界配置单独校验。
- 新增 `voice_input_gate_test` 和配置回归测试，覆盖 KWS 关闭直通、KWS 命中进入 Listening、cooldown
  抑制重复唤醒以及可配置唤醒词解析。

## 2026-08-14 - USB 声卡实机采集适配

- 开发配置绑定 Texas Instruments PCM2902 的稳定 ALSA 名称
  `plughw:CARD=Device,DEV=0`，保持 16 kHz、单声道、20 ms PCM 合同，生产配置不变。
- 修复 non-blocking ALSA capture 在 `PREPARED` 状态先 poll、尚未开始产生帧的问题；capture
  完成硬件参数配置后显式 `snd_pcm_start()`，播放路径不变。
- 使用项目 `audio-probe` 在真实 USB 声卡上完成 3 秒录制和回放，共 48,000 帧；`null` 开发设备
  回归仍可采集 16,000 帧。

## 2026-08-14 - 确定性硬件命令正向白名单封板

- `DeterministicCommandRouter` 改为对规范化后的完整 transcript 匹配固定 `CommandEntry`
  正向白名单；只有明确列出的 27 种命令形式可以产生现有三种 `VoiceAction`，其余输入全部
  fail closed 为 `Unknown + None`。
- 删除自由文本 action substring、英文 word-boundary、否定词上下文和危险参数 blacklist
  匹配。否定、条件、延期、意愿表达、多动作组合和未知自然语言不再依赖枚举拒绝词，而是因为
  不属于正向白名单直接拒绝。
- 补齐全部允许命令及 can't/cannot/not、不能/禁止/无需/不必、条件句、组合句和嵌入式正向
  phrase 的拒绝测试。Voice Agent 阶段 10 第一批至此封板，后续不继续扩展本批规则。

## 2026-08-14 - 命令否定语义与 Safe OTA timeout 合同加固

- 确定性路由不再全句扫描少量否定词；每个 action phrase occurrence 分别检查有限前置上下文。
  中文支持不、不要、别、请勿、不用和不需要，英文支持 don't、do not 和 never；只要存在被否定
  action，即使同句还有肯定 action，也保守返回 `Unknown + None`。
- `TranscriptNormalizer` 将 U+2018/U+2019 smart apostrophe 规范化为 ASCII `'`，使两种
  `don’t/don‘t` 稳定进入同一个英文否定路径，不引入 Unicode 大依赖。
- Safe OTA runtime readiness 改为接收 caller steady-clock deadline；每次 status probe 与 inotify
  poll 均取剩余预算，候选和 rollback readiness 都不再越过 `--timeout` 等待内部固定 30 秒。
- 新增中文/英文否定、smart apostrophe、否定与肯定组合歧义，以及永不创建 runtime socket 的
  1 秒 caller budget 回归测试；保留现有 readiness watch、诊断和事务安全语义。

## 2026-08-14 - Voice Agent 阶段 10 第一批确定性命令边界

- 新增最小 `TranscriptNormalizer`，支持首尾/连续空白、ASCII lowercase、全角 ASCII、U+3000 和
  常见中英文标点规范化，同时原样保留有效中文 UTF-8。
- 新增 `DeterministicCommandRouter`，第一批只路由已有的打开相机、播放音乐和查询车辆状态三种
  `VoiceAction`；英文匹配要求合理 word boundary，中文只匹配明确短语。
- 否定短语、多个动作同时命中、未支持的速度/转向/油门/制动参数和英文长词子串全部 fail closed，
  返回 `Unknown + None`，不增加 Slot、Entity、参数 Action、DSL 或通用 NLU 框架。
- `MockVoiceAssistant` 保持现有 API 和 deadline 行为，只把内部文本处理替换为 normalizer/router；
  Stage 6 lifecycle、ActionDispatcher allowlist 和 driver/library 分层未修改。
- 阶段 6 继续保持正式封板；这里只完成阶段 10 第一批，不代表真实 ASR、LLM 或整个阶段 10 完成。
- WSL 验证：Debug 56/56、Release 56/56、ASan/UBSan 非 system-grpc 48/48、当前 CI TSan regex
  9/9；新增 router 测试也在 TSan 下通过，driver dependency boundary 和 pre-commit 通过。

## 2026-08-14 - Voice Agent 阶段 6 正式封板

- `AudioPlaybackClient` 将同一 playback id 的取消改为 single-flight：并发 Stop、Interrupt 和 Submit
  cancellation 共享一个实际 Cancel RPC；只有明确失败后才在同一 owner 内进行最多一次重试，
  completion 保持 exactly once。
- Action deadline 扩展为最小 request-scoped execution context。Interrupt 先标记取消 token，
  dispatcher、vehicle/HMI provider 在提交 RPC 前复查；已活动的 gRPC 继续使用 `TryCancel`。
- `SpeechPipeline` 增加 lifecycle generation，Stop 后即使不服从 Cancel 的 recognizer 返回 success，
  也不会发布 stale transcript。
- driver dependency boundary 删除 `rg || true` 假通过路径；CI 在执行检查前安装 ripgrep，并增加缺少
  工具、向上 include、项目 CMake target 和正常目录四类自动化验证。
- 阶段 6 到此正式封板；下一主线是阶段 10 的 TranscriptNormalizer、确定性命令路由、否定词和参数
  边界，不继续扩展阶段 6。
- WSL 验证：Debug 55/55、Release 55/55、ASan/UBSan 非 system-grpc 47/47、当前 CI TSan regex
  9/9；TSan `audio_playback_client_test` 额外连续 20 次通过；真实 driver boundary 和签名 package
  validation 通过。

## 2026-08-14 - Voice Agent 阶段 6 通用运行时收尾

- 删除通用 `request_timeout_ms`，活动配置改为 ASR、Assistant、Action、TTS synthesis 四个独立预算；
  所有业务 deadline 使用 steady clock，Gateway/HMI 只根据剩余预算设置 gRPC deadline。
- ASR、Assistant 和 Action 的 watchdog 在超时后取消实际 provider 并拒绝 stale 结果；Action cancel
  能路由到当前实际使用的 vehicle 或 HMI provider，HMI active gRPC 支持 `TryCancel`。
- Assistant/provider failure 与 timeout 复用既有 VoiceResponseSink 播放固定错误提示；恢复提示期间
  保持 `ERROR_RECOVERY` active，完成/失败/打断后回到 `IDLE`，播放自身失败不会递归生成提示。
- TTS synthesis 预算与 PCM playback 生命周期保持分离；accepted playback 遇到 Timeout 或
  TransportError 时执行最多两次取消和一次有界 terminal confirmation，最终不确定时明确失败，
  不伪报 `Cancelled`。
- 超时指标拆分为 `asr_timeouts`、`assistant_timeouts`、`action_timeouts` 和 `tts_timeouts`；新增配置
  非正值、旧字段、provider 总预算/取消、stale 结果、恢复提示和 transport uncertainty 测试。
- 阶段 6 只宣告通用运行时逻辑完成；真实 ASR、TTS、LLM、麦克风、扬声器及声学验证仍未完成。
- WSL 验证：Debug 54/54、Release 54/54、ASan/UBSan 非 system-grpc 46/46、当前 CI TSan regex
  9/9、driver dependency boundary、签名 package validation 和 pre-commit 全部通过。

## 2026-08-13 - Voice Agent 真实播放与 FOLLOW_UP 闭环

- `AudioControl.PlayPcm` 增加 playback id，继续只表达是否入队；新增最小的单次结果等待和取消 RPC，
  不引入 streaming 或通用异步框架。
- `AudioPlayback::Run()` 在真实 `AudioPlayer::Play()` 返回后生成 completed、failed、cancelled 或
  dropped 最终结果；结果历史和播放队列均保持有界。
- Voice 输出接口增加 exactly-once completion 和独立 `Interrupt()`；Agent 在入队成功后保持
  `SPEAKING`，真实完成后进入 `FOLLOW_UP`，失败走现有恢复链路。
- `FOLLOW_UP` 使用 steady-clock 8 秒窗口；新 transcript、interrupt 和 shutdown 会使旧 timer 与旧
  completion 失效，避免旧会话改变新会话状态。
- 修正文档中的历史 ALSA 依赖示例：最终方向为 Linux/ALSA → drivers → modules → library，上层依赖
  下层，driver 不链接 core/modules/library/agent。
- WSL 验证：Debug 53/53、Release 53/53、ASan/UBSan 非 system-grpc 46/46、当前 CI TSan regex
  9/9、driver dependency boundary 和 pre-commit 全部通过。

## 2026-08-13 - Driver、CAN FD、运行时故障与语音 deadline 收口

- ALSA、SocketCAN driver 改为只依赖 STL 和 Linux/ALSA 系统接口；领域适配移到 modules，CI 新增
  driver include/link 方向门禁。
- SocketCAN 启用 CAN FD 和 error filter，支持 0–64 Byte、BRS、ESI、标准/扩展 ID；CAN 链路
  idle、bus-off、error-passive/warning、ACK 和 protocol error 指标通过 vehicle gRPC 与
  `cockpit-ctl health/status` 暴露。车辆信号 codec 仍明确是原型，等待 STM32G474 正式协议。
- Audio capture 启动只在 Running 时成功，运行期 Fault 会传给 Navigator Poll；mode stop 失败会恢复
  原 mode 的模块集合。
- 生产 release/current 改为 root 管理且只读，Navigator systemd 只开放 data/logs/run；事务恢复由
  root 管理员或后续外部升级协调器执行，不扩大运行进程写权限。
- ASR、LLM、TTS 接口统一 deadline/cancel，超时 watchdog 主动取消 provider；新增有界停止测试和
  `SOCK_SEQPACKET PCM → VAD → ASR → transcript → Agent` 端到端测试。

## 2026-08-13 - 文档入口收口

- 将常用文档统一为简短英文文件名并平铺到 `docs/`，只把时点材料保留在 `reference/`，减少后续
  对话定位和读取成本。
- 将语音阶段 0–15 的四个分册重新合并为 `voice-agent-tasks.md`；当前交接信息置顶，可按阶段标题
  定位，无需读取整份任务表。合并前后正文行数已校验，独有内容未删除。
- `voice-agent.md` 只保留稳定架构，阶段状态和验收只在任务表维护；`status.md` 和 `roadmap.md`
  分别作为当前事实与下一步的唯一入口。
- 校正 Agent/Audio 当前路径和职责、Sherpa/ONNX Runtime 产品依赖边界、配置 RPC 描述、部署依赖
  策略、脚本名及 WSL/Jetson 测试快照。

## 2026-08-13 - 语音 Agent 会话状态机

- 将《语音与 AI 规划》合并到 `voice_agent_architecture.md`，按“稳定架构”和
  “分阶段实施计划”拆分，删除重复文档并更新文档索引。
- 根据阶段任务表将 0–5 标记为已完成，阶段 6 标记为进行中；同时纠正
  “恢复算法级动态插件”与已定架构的冲突。
- 新增 `agent/conversation/ConversationStateMachine`，用显式事件驱动唤醒、监听、
  识别、路由、执行、思考、播放、跟进、取消、错误恢复和停机状态。
- `VoiceInteractionService` 接入状态机，拒绝并发会话和非法转换，provider 失败、
  超时和打断后清理过期队列并恢复 `IDLE`，停止后保持 `SHUTTING_DOWN`。
- Voice RPC 新增细分状态、最后转换原因、成功/拒绝转换指标，CLI 和 smoke
  测试同步更新。
- Debug 和 Release 全量 CTest 均 48/48 通过；ASan/UBSan 阶段 6 聚焦测试 4/4
  通过；`pre-commit run --all-files` 通过。

## 2026-07-30 - Audio Driver 与 Agent 分层

- 明确 `cockpit/drivers` 只负责硬件适配，`cockpit/library/driver` 只负责驱动模块组装和数据
  传输；VAD、语音分段、ASR、LLM 和 TTS 归入顶层 `agent/`。
- 新增版本化、固定长度和显式小端序的 PCM 线协议，以及 Driver 侧有界
  `AudioStreamPublisher` 和 Agent 侧 `AudioStreamClient`。
- PCM 第一阶段使用 Unix `SOCK_SEQPACKET`；发布线程与采集线程分离，队列溢出通过
  discontinuity/dropped-before 标记显式传递。
- `alsa_audio` 改为只链接 capture、frames 和 playback 最小 target，不再通过 `audio` 聚合
  target 间接依赖 VAD。
- Driver Publisher 与 Agent Client 已接入真实模块运行链路；VAD、分段、ASR、TTS 和交互
  Runtime 全部归入顶层 `agent/`。
- 删除 VAD/ASR C ABI、动态加载器、插件 fixture 和 `plugin_path` 配置；基础仓库只保留 mock
  实现，真实算法后续作为 Agent 产品 target 接入。
- Audio gRPC 删除 `Speak(text)` 和 transcript stream，改为纯 PCM `PlayPcm`；transcript
  改由 Agent 的 `VoiceInteractionControl` 发布。
- Driver 侧 `AudioService`/`SpeechOutput` 分别收口为 `AudioCaptureController` 和
  `AudioPlayback`，不再持有任何语音算法或文本语义。
- WSL GCC Debug 全量 CTest 47/47 通过；Release 全量构建和 39 个非 socket 测试通过；
  ASan/UBSan 全量构建和 38 个非 socket/system-gRPC 测试通过。受执行器 ptrace 限制，本地
  Sanitizer 测试关闭 LSan，CI 继续按既有矩阵运行。

## 2026-07-30 - VAD 插件边界与语音架构

- 重构车载语音与本地 Agent 架构文档，明确区分当前实现、Jetson 目标基线和候选实验，并固定
  单 systemd service、Navigator 子进程监管、依赖隔离、内存护栏和分阶段验收边界。
- 删除生产链路中的 `EnergyVad` 和基于 dBFS 的判定配置；RMS、峰值和削波提取为独立
  `AudioLevelMeter`，只用于输入诊断。
- 新增纯 C `CockpitVadPluginApi`、`RTLD_NOW | RTLD_LOCAL` 加载器和 ABI fixture；VAD 插件接收
  PCM chunk，可在内部适配 Silero/TEN-VAD 的模型窗口。
- `AudioService` 改为注入通用 `VoiceActivityDetector`，不再创建或配置具体 VAD；插件选择和加载
  只在 `AudioRuntime` 入口处理。
- 活动 YAML 的 VAD 配置收口为 `disabled/plugin` provider 和严格绝对路径；语音功能启用时必须
  配置真实 VAD 插件。
- GCC Debug 全量编译通过；放宽 Unix socket 沙箱限制后全量 CTest 48/48 通过。

## 2026-07-29 - 通用 ASR 插件边界

- 恢复与厂商无关的 `mock/plugin` ASR 配置；插件路径和插件配置路径使用严格绝对路径校验。
- 新增纯 C ABI，PCM 输入和结果缓冲区均由调用方持有，不跨动态库传递 C++ 类型或释放内存。
- 外部 ASR 使用 `RTLD_LOCAL` 加载；成功调用后保留映射至进程退出。
- 纯 C fixture 覆盖 ABI 版本拒绝、配置传递、PCM/flags 转换和识别结果转换。

## 2026-07-29 - 移除内置 Sherpa-ONNX

- 删除 Sherpa-ONNX、ONNX Runtime、SenseVoice 的下载、构建、链接、配置、测试和发布元数据。
- 语音链路暂时只保留 mock ASR；项目依赖全部由 Ubuntu 22.04 系统包提供。
- CI 不再 bootstrap 或缓存第三方 ASR 源码；ASan/UBSan 跳过与系统 gRPC 1.30.2 不兼容的运行测试，
  这些测试继续由 Debug 和 Release 矩阵覆盖。
- 故障注入使用 sanitizer 不接管的信号，保持 Navigator 崩溃重启测试在普通构建和 ASan 中一致。

## 2026-07-29 - Navigator 产品策略与生命周期收口

- 参考 Zelos Navigator 的产品策略边界，将模块和 mode 分别收为 `ModuleId`、`RunMode`；模块名称
  与动态库、mode 与模块集合统一由 `navigator/run_config` 维护，字符串只在 CLI/IPC 边界解析。
- 为模块和 mode 分配稳定显式 ID；Navigator 启动前统一校验模块、动态库、mode、Socket、超时和
  重启策略，产品策略错误时拒绝启动。
- 重启上限和非阻塞重启延迟改为逐模块策略；关键模块耗尽重启次数后让 Navigator 失败退出，普通
  模块保持 failed 供诊断。
- module child 设置可识别的 Linux 进程名；runtime status 增加启动次数、运行时长、最近失败时间
  和终止信号。

## 2026-07-29 - 工程脚本内容收口

- 删除 normal/development/cloud 三个 systemd target 和 `cockpit-navigator@.service` 模板，量产
  只安装固定的 `cockpit-navigator.service`，始终从 normal mode 启动。
- 安装脚本统一创建 `cockpit` 系统账号和运行目录；安装旧包升级时迁移旧 unit
  的 enabled/active 状态，并删除旧 target、模板和实例。
- `build.sh` 使用绝对源码目录和默认工具链路径，可从仓库外执行，不再隐式依赖当前工作目录。
- `package.sh` 先确认并刷新现有构建目录，再读取 CMake 专门生成的发布元数据；增加版本输入
  校验，避免无效参数进入发布路径。
- `lint.sh` 和 WSL matrix 改读 `package-info.env`，不再解析 `CMakeCache.txt` 内部字段。
- Jetson sysroot 默认写入仓库 `_output/sysroots/jetson`，不再因调用目录不同而落到未知位置。
- 保留 smoke、WSL matrix 和稳定性脚本的完整场景流程，不为减少行数新增通用运行框架。
- `scripts/tests/` 统一保存 smoke、稳定性、WSL、OTA 和诊断等 Shell 测试入口；`tests/`
  只保留按领域组织的 C++ 测试源码与 fixture，避免测试脚本散落在多个代码目录。
- 只有一个公共 Shell 文件，不再单独保留 `scripts/lib/`；路径函数收到不可执行的
  `scripts/common.sh`。
- 删除自定义 `JOBS`；构建并行度交给 Ninja/CMake，外部需要限制时使用标准
  `CMAKE_BUILD_PARALLEL_LEVEL`。
- 原生构建入口显式恢复项目 `BUILD_TESTING=ON`，避免 Sherpa-ONNX 依赖下载中断后遗留的缓存关闭
  项目测试；CTest 使用 `--no-tests=error`，禁止“零测试”被误报为成功。
- WSL matrix 将临时安装树放在 `/tmp`（可由 `WSL_MATRIX_WORK_PARENT` 覆盖），避免深层输出目录
  使 Unix Socket 超过 108 字节；成功时清理临时工作目录，失败时保留安装树和日志，避免错误
  信息指向已经删除的 `navigator.log`。
- WSL matrix 在自定义 `COCKPIT_ROOT` 安装后同步调整临时活动配置中的部署根路径；正式发布配置
  继续固定使用 `/cockpit-system`。WSL 活动配置使用 mock 车辆源，不依赖 Jetson 的 `can0`；
  第二次安装仍验证用户配置不会被模板覆盖。
- 安装脚本将已验签的发布 manifest 一并保存在版本目录，确保健康检查可从
  `current/manifest/VERSION` 校验正在运行的 Navigator 版本。
- 修正部署健康检查提取运行版本和可执行文件路径时的 `sed` 分组表达式。
- `BUILD_INFO.json` 区分 OTA 发布号 `version` 与编译进可执行文件的 `binary_version`；健康检查
  使用后者校验进程，回滚目录继续使用前者，支持同一构建产物的部署矩阵版本。
- 随发布包交付的文件归入根目录 `deploy/`；工程脚本统一使用连字符命名。

## 2026-07-28 - 工程辅助目录按职责收口

- `scripts/` 保存构建、打包、环境准备和本地运行入口，共享路径函数放在
  `common.sh`。
- 安装、回滚、发布健康检查、systemd 单元和第三方声明统一归入根目录 `deploy/`；Shell 测试
  入口统一归入 `scripts/tests/`。
- 应用配置明确命名为 `configs/development.yaml` 和 `configs/production.yaml`；部署后仍使用
  `/cockpit-system/config/config.yaml`，不改变升级保留合同。
- `configs/` 只保存程序读取的 YAML；配置说明统一维护在 `docs/配置说明.md`，不在配置目录重复
  放置 README。
- `scripts/` 和 `deploy/` 不再重复放置 README；脚本用法统一由根 README 与
  `docs/部署说明.md` 维护，目录中只保留入口、公共函数和发布资产。
- `cmake/` 收口为 `project_options.cmake`、`dependencies.cmake`、`install.cmake` 和
  `toolchains/`；包元数据归入安装发布职责，不再单独暴露实现文件。
- 删除 `add_cockpit_library()` 包装层，业务目录统一使用标准 `add_library(... STATIC ...)`；
  工程 include 和 warning 规则由 `cockpit_project_options` interface target 继承。
- Jetson 工具链同时指定 ARM64 C/C++ 编译器，避免 C 源码误用宿主机 GCC；删除无 target 使用的
  `BUILD_WEB_DASHBOARD` 旧选项。
- 根 README 和部署文档集中维护辅助目录职责与入口，并同步更新代码默认值、测试、CI、pre-commit
  和用户文档中的路径。
- WSL GCC Debug 全量构建通过；CTest 46/46 通过。测试签名包的目录、脚本语法、SHA-256 和
  Ed25519 签名验证通过，临时包和密钥已清理。

## 2026-07-28 - Jetson 默认关闭 HMI

- `normal` 和 `development` mode 不再自动启动 HMI/Qt UI，新增仅供手动验证的显式 `ui` mode。
- systemd 移除 Xorg 环境文件和 `CAP_DAC_READ_SEARCH`，部署脚本不再创建旧 Whisper 模型目录。
- systemd 使用无登录、无密码的 `cockpit` 系统用户运行，安装脚本按实际存在的设备组授予硬件访问。
- 首次安装通过外部 OTA 公钥验证发布包并固定信任，不再要求预先手工创建安装目录。
- 打包前重新执行现有构建目录的 CMake 配置，确保 Git revision 和 dirty 状态不是旧缓存。
- UI 保持 1280×720 普通窗口；Jetson 图形栈长稳通过前不加入开机启动路径。

## 2026-07-28 - 系统依赖与 ONNX Runtime 隔离

- 应用层改用 Ubuntu 22.04 系统包提供的 Protobuf 3.12.4 和 gRPC 1.30.2，不再构建、缓存或打包
  项目专用的 gRPC/Protobuf 前缀。
- Sherpa-ONNX 继续自行获取静态 ONNX Runtime，Navigator 模块保留符号隐藏和 `RTLD_LOCAL`，
  ONNX Runtime 的依赖不会进入主进程动态依赖。
- 第三方 bootstrap 仅准备 Sherpa-ONNX 源码；安装包仅包含项目 target，系统动态库由 apt 管理。
- 全新无缓存构建、安装和签名打包通过，Debug CTest 46/46。

## 2026-07-27 - 全仓库审核整改

### 变更内容

- OTA 激活后完整 `exec` 新 Navigator，健康检查校验 Navigator 与全部 module child 均来自当前
  release；安装、校验和健康命令统一使用有超时/取消的进程组。
- OTA 包新增 Ed25519 manifest 签名和语义版本 anti-rollback，确认版本持久化到
  `data/ota-version-floor`。
- Navigator IPC 增加单实例锁、socket inode 所有权和完整流式 framing。
- Recording 增加活动会话字节/时长/剩余空间限制、data-file allowlist、分流 flush 和关键目录
  fsync；Camera 修复并发状态访问、故障上报及 JPEG 原子防覆盖写入。
- 发布配置使用 Unix gRPC socket；systemd 增加 sandbox，并仅保留读取 Jetson Xauthority 所需的
  `CAP_DAC_READ_SEARCH`。
- 新增固定版本第三方依赖 bootstrap、GitHub CI、ASan/UBSan 与 Camera TSan 构建入口。

### 验证结果

- Jetson ARM64 Debug 46/46、Release 47/47 CTest 通过，Release 包完成 Ed25519 签名打包。
- Navigator、Recording、Camera 定向测试通过，测试结束后无遗留 module child 或共享内存锁。
- Safe OTA 集成测试覆盖签名篡改、payload 篡改、anti-rollback、卡死健康命令清理、完整进程树切换
  和失败回滚。

## 2026-07-27 - Jetson 本地工作区收口

- 模型统一按 `models/asr`、`models/tts` 和 `models/downloads` 分类，迁移前后 32 个文件的
  SHA256 集合一致；生成的 WAV 统一放入 `_output/runtime/audio`。
- 项目根目录旧日志合并到 `_output/runtime/logs`，清理空的 `logs`、`run` 和
  `cockpit/library/driver/audio/asr` 目录。
- TTS 基线实验迁入 `_output/experiments/sherpa-onnx` 并使用 Ninja 重新构建；移除
  `third_party/sherpa-onnx` 下两份失效的旧构建缓存。
- 新增本地工作区、`library`、`library/driver` 和 `library/agent` 职责说明；保留
  `library` 与完整 `driver` 入口层，不改为 service 目录。

## 2026-07-23 - Jetson 单版本构建收口

- 固定构建 HMI、GStreamer 和 sherpa-onnx + SenseVoice，运行时 ASR 只保留 `mock` 与
  `sherpa_onnx_sense_voice`；移除 Whisper 构建入口和实现。
- 项目自有 C/C++ 不再使用 `#if`、`#ifdef`、`#ifndef`、`#elif`、`#else`、`#endif` 条件预处理；
  没有增加 provider 工厂、生成配置头或 unavailable 实现文件。
- 固定使用 Protobuf 3.21.12 和 gRPC 1.51.3，打包流程始终携带同一前缀的运行库。

## 2026-07-23 - Navigator 本地崩溃记录

- Navigator 在运行中 module child 异常退出后写入独立 JSON 记录，包含模块、mode、旧 PID、退出码、
  signal、core 标志、重启结果、重启次数和新 PID；正常停止、显式重启和 mode 切换不生成记录。
- 记录默认写入 `paths.data_dir/crashes/`，固定最多保留 20 份并删除最旧文件；不抓取 core、不复制
  配置或完整日志，也不新增服务和活动配置字段。
- 新增独立保留策略测试，并扩展 Navigator 集成测试覆盖信号故障、成功重启、达到重启上限和干净
  关闭；Jetson ARM64 GCC Debug/Release 全量 CTest 均为 46/46。

## 2026-07-22 - Jetson sherpa-onnx + SenseVoice INT8

- 新增默认关闭的 `BUILD_SHERPA_ONNX_ASR` 和 `sherpa_onnx_sense_voice` provider，复用现有
  `SpeechRecognizer`、VAD 分段、线程数、语言和模型路径配置；`tokens.txt` 从模型同目录读取。
- 启用 SenseVoice 的构建统一使用 Protobuf 3.21.12 和 gRPC 1.51.3，不再混用系统 Protobuf
  3.12；sherpa-onnx 直接链接到现有 `audio_driver`，没有新增服务或独立 provider 进程。
- Jetson Orin Nano Super 使用 sherpa-onnx `13d0ae6c` 和 SenseVoice INT8 识别 5.59 秒中文 WAV，
  输出“开放时间早上9点至下午5点。”；初始化约 2.68 秒、识别约 0.42 秒、RTF 约 0.075、采样峰值
  RSS 344644 KiB。模型 SHA256 为 `c71f0ce00bec95b07744e116345e33d8cbbe08cef896382cf907bf4b51a2cd51`。
- ARM64 GCC Release 完整 CTest 46/46，全部动态依赖均为 `libprotobuf.so.32`，未混入系统
  `libprotobuf.so.23`。0.1.3 暂存包包含同一前缀的 Protobuf/gRPC 共享运行库，模块通过相对
  RPATH 从包内加载；包大小约 75 MB，压缩后约 25 MB。模型保持包外，当前尚未部署这版。

## 2026-07-21 - Jetson systemd 正式部署

- ARM64 Release `0.1.2` 正式安装到 `/cockpit-system`，`cockpit.target` 已启用并托管 normal mode；
  Xorg 的 `DISPLAY`、`XAUTHORITY` 通过保留型 `/cockpit-system/config/environment` 持久配置。
- 管理员 healthcheck 在 systemd 重启前后均确认 gateway、audio、voice、camera 正常；服务保持
  `active/running`、`NRestarts=0`，HMI/UI 无 Xorg 连接错误。
- 回滚到 `0.1.1` 后重启和 healthcheck 通过，再恢复 `0.1.2` 后同样通过；三个 release 目录均保留。
  开机 target 已 enable；随后完成整机冷启动验证，boot ID 从
  `0c26fde6-2a6d-482a-ae8f-07ed1a6d0908` 变为 `cc9012a2-4568-4918-be10-ed96bd83ad82`，
  `cockpit.target` 与 normal mode 均自动进入 active，版本保持 `0.1.2`、`NRestarts=0`。
- 冷启动后 Navigator 正常拉起 transfer、vehicle/audio/camera driver、agent、HMI 和 `cockpit-ui`；
  gateway、audio、voice、camera health 全部通过，IMX219 仍只枚举为 `nvargus://0`。
  异常掉电和 systemd 小时级长稳仍待单独验证。
- `0.1.2` 正式设备列表只暴露 `nvargus://0`；IMX219 三秒达到 101 帧、0 丢帧、0 source gap，
  并保存 640×480、113618 字节 JPEG。
- 正式 systemd 进程继续运行 IMX219 7 分 33 秒，最终达到 13599 帧、0 丢帧、0 source gap；
  camera driver RSS 从 85820 KiB 变为 85840 KiB、线程从 20 变为 19，结束 JPEG 为 640×480、
  86292 字节，四项 health 均通过。小时级长稳仍保留为后续验证。

## 2026-07-21 - Jetson CSI 设备发现

- camera service 识别 `tegra-video`、`platform:tegra-capture-vi` 的 Jetson CSI 节点，并按枚举顺序
  将其转换为 UI 可直接使用的 `nvargus://<sensor-id>`；USB/V4L2 摄像头继续保留 `/dev/video*`。
- 修复正式 systemd 部署中 UI 只显示 `/dev/video0`、IMX219 启动后因 Bayer V4L2 pipeline
  `not-negotiated` 而 faulted 的问题。
- 新增双 CSI 节点到 Argus sensor 0/1 的单元覆盖；正式 `0.1.1` 部署手动切换
  `nvargus://0` 后达到 300 帧、0 丢帧，gateway/audio/voice/camera gRPC 健康全部通过。

## 2026-07-21 - Jetson mttcan 内部闭环

- `run_vcan_smoke.sh` 新增 `CAN_SKIP_INTERFACE_SETUP=true`，可复用由管理员预先配置并启用的
  真实 SocketCAN 接口，不再强制执行需要 sudo 的 `vcan` 创建步骤。
- Jetson `mttcan can0` 以 500 kbit/s、controller loopback 运行端到端 smoke；3 帧均成功收发，
  CAN 错误、丢包和 bus-off 均为 0，Navigator 发布的 VehicleState 标记为 `source=socketcan`。
- 该结果验证了 mttcan 驱动、SocketCAN adapter、车辆解码和 gateway/topic 链路；载板没有板载
  CAN 收发器，外部 CAN_H/CAN_L、正式车辆信号和断线恢复仍需接入外置收发器后验证。

## 2026-07-21 - 录包事件发布器不可达降噪

- recording module 未启动时，camera/agent 的 best-effort 事件发布不再逐条输出连接失败警告；首次
  不可达只记录一次状态转换，此后在 1 秒重试窗口内直接丢弃事件，恢复后汇总记录丢弃数量。
- gRPC channel 的重连退避限制为 200 毫秒至 1 秒，兼顾 normal mode 降噪与运行中切换到
  development mode 后的快速恢复。
- IMX219 normal mode 连续运行 20 秒只产生 1 条不可达警告、0 条旧式逐事件警告；切换到
  development mode 后的 7 秒录包共写入 41 条消息，其中包含 6 条每秒采样的相机帧元数据。

## 2026-07-21 - Jetson IMX219 Argus 预览

- GStreamer 相机后端新增显式 `nvargus://<sensor-id>` URI，使用 `nvarguscamerasrc`、NVMM 和
  `nvvidconv` 获取 Jetson CSI 帧；普通 `/dev/video*` 继续走 V4L2，不按平台误判 USB 摄像头。
- `CameraService` 接受 Argus URI 并交给采集后端验证；`run_camera_ui.sh` 在 Jetson 上默认选择
  `nvargus://0`，也可通过 `CAMERA_DEVICE=/dev/video0` 验证 USB 摄像头。
- IMX219 在 Xorg 真机 UI 中以 640×480@30 连续预览，状态采样 271 帧、0 丢帧，并成功保存
  640×480、76699 字节 JPEG。CSI 故障恢复和长稳仍待验证。
- 两分钟实载采样最终达到 4146 帧、0 丢帧、0 source gap，结束 JPEG 有效；切换到不存在的
  `nvargus://99` 后 watchdog 正确报告 `faulted/no_frames`，恢复 `nvargus://0` 后 3 秒内达到
  100 帧且 `recover_count=1`。小时级长稳仍待验证。

## 2026-07-21 - Jetson ARM64 原生基线

- 在 Jetson Orin Nano Super、L4T R36.4.3 上完成 GCC Debug 和 Release 原生构建、ARM64 打包及完整 smoke。
- `hmi_runtime_test` 不再依赖 Jetson 内核未提供的 `/proc/<pid>/task/<pid>/children`，改用 `pgrep -P`
  查询 `cockpit-ui` 子进程；HMI/UI 崩溃恢复测试在 x86_64 和 Jetson 上使用同一检查路径。
- Debug 与 Release CTest 均为 45/45 通过；完整 smoke 和临时部署矩阵通过，ARM64 双版本安装、
  healthcheck、包篡改拒绝、故障恢复与 rollback 均正常；同时确认 IMX219 `/dev/video0` 与 Jetson
  APE/HDA ALSA 端点可枚举。真实采集、播放、CAN、图形显示、systemd 和长稳仍需分别验证。
- Jetson 系统包提供的 pre-commit 2.17.0 无法解析当前 hook stage；已在用户环境升级到 pre-commit
  4.6.0 和 virtualenv 21.6.1，本批修改文件门禁通过。

## 2026-07-17 - 编译工具链统一为 GCC

- `build.sh` 的 Debug、Release 和 ARM64 交叉构建统一使用 GCC，不再提供 Clang 编译选项。
- `clang-tidy` 保留为静态检查工具，改为读取 GCC Debug 生成的 compile database。
- WSL matrix 同时校验 Debug 和 Release 构建的编译器身份为 GNU。
- GCC 11.4 Debug 独立构建成功，完整 CTest 45/45 通过。

## 2026-07-17 - WSL-R4 Whisper 真实识别基线

- 可选 whisper.cpp 使用独立 Clang Debug 和 GCC Release 构建目录，不改变默认构建，也不把约
  465 MiB 的模型提交或打入发布包。
- `build.sh` 同时选择同族 C/C++ 编译器，避免启用含 C 源码的可选依赖时出现 GCC/Clang 混用。
- 基线固定 whisper.cpp `6fc7c33b`、模型 SHA1 `55356645c2b361a969dfd0ef2c5a50d530afd8d5`
  和 16 kHz mono JFK WAV；GCC Release 正确输出完整英文句子。
- GCC Release 耗时 4.39 秒、CPU 393%、峰值 RSS 649232 KiB；Clang Debug 无 OpenMP 时耗时
  50.15 秒、峰值 RSS 650944 KiB，Debug 数据只用于开发诊断，不作为性能结论。
- 现有条件 CTest 在 GCC Release 下 1/1 通过；WSL-R1 至 WSL-R4 至此全部完成。

## 2026-07-17 - WSL-R3 长运行资源基线

- `run_navigator_stability.sh` 将报告升级为 schema 3，每次采样记录 Navigator 与运行中 module child
  的 RSS、线程数、FD 数，并记录日志、录包、报告和诊断快照目录占用。
- 报告汇总资源初值、峰值和结束值；`--fault-count` 支持重复 restart/crash 注入，事件逐次记录
  before/after PID 和恢复结果。未启动的预留模块不参与 `/proc` 统计。
- 资源读取只保留 `/proc` 和文件系统两个边界函数，没有新增 Runtime 类、后台线程或独立工具；资源
  样本不可用会让稳定性检查失败，目录增长只提供事实数据，不引入缺乏实机依据的任意阈值。
- CTest 连续 3 次 crash/recovery 通过。normal/development 各 30 秒基线均恢复 3/3 次故障且无采样
  缺失；RSS 分别增加 604 KiB、928 KiB，FD 分别减少 3 个，录包、报告和快照占用不增长。

## 2026-07-17 - WSL-R2 活动配置合同收口

- 删除旧 `runtime.dependencies`、`cockpit-ctl dependencies` 和只被测试使用的重复依赖图；Navigator
  的模块组合、启动顺序和依赖只由代码内 `run_config` 维护。
- 从 `SystemConfig` 和活动 YAML 移除没有真实 consumer 的 WebSocket、cloud uplink、AI/TTS provider
  以及固定实现选择字段；严格配置校验会拒绝这些旧键。
- `transfer` 和 `carupload` 不再打印未实现的 WebSocket/MQTT 计划；cloud mode 只验证 carupload ABI
  生命周期，不伪装成已上传数据。
- WebSocket、MQTT/TLS、TTS/LLM provider 等未来契约保留在现有配置和语音规划文档中，等真实实现与
  后端合同明确后再进入活动配置。
- x86_64 Debug 全量增量构建、45/45 CTest 和完整 smoke 通过；CTest 清单由 46 项变为 45 项，
  减少项为已删除的重复依赖图自测。
- 全量回归发现 safe-ota 的同步 mode 切换会合法超过普通 IPC 的 1 秒响应预算；普通调用继续保持
  1 秒有界等待，仅 safe-ota 显式使用 30 秒有界响应等待。

## 2026-07-17 - WSL-R1 故障自动留证

- `run_navigator_stability.sh` 在 Navigator 未就绪、健康采样失败、故障恢复失败、进程退出或脚本异常时
  自动生成一次诊断快照，schema 2 报告记录原因、可用状态、快照路径和 Navigator 日志路径。
- `cockpit-ctl snapshot` 增加 `--max-snapshots` 和 `--max-total-bytes`，默认保留 10 份、合计
  100 MiB；快照原子发布后按修改时间删除最旧目录。
- 复用现有诊断快照测试覆盖数量和总字节上限；定向编译以及 `diagnostic_snapshot_test`、
  `navigator_stability_test` 通过。受限沙箱的 Unix Socket 启动失败也验证了 `runtime_not_ready`
  自动报告和快照路径。

## 2026-07-17 - 当前文档与动态运行时同步

- 同步架构、通信、配置、语音、部署、状态看板和各级 README，统一使用 Navigator 管理的进程级
  模块描述，不再把 gRPC service 接口名写成独立后台进程。
- 补齐有界 Navigator IPC、录包 artifact/完整性报告、诊断快照、日志和当前工具链的实际状态。
- 明确 WebSocket、AI provider、cloud 开关、旧 service 依赖表、carupload 和预留模块仍未形成可用能力，
  并将 WSL 后续主线收口为失败留证、活动配置、长运行资源和可选 Whisper 基线。
- 保留既有历史批次原文，本批只修正描述当前状态和使用方式的文档。

## 2026-07-17 - Navigator IPC 有界等待

- Navigator 本地 IPC 客户端的连接、写入和响应读取增加 1 秒超时，服务端回复同样不再无限阻塞。
- 复用现有 `navigator_test`，覆盖 peer 接受连接后不响应的场景；定向编译和测试通过。

## 2026-07-17 - 执行与验证克制规则

- `代码风格.md` 将克制原则扩展到任务范围、测试、验证和文档，要求新增内容对应真实复杂度或风险。
- 本批仅运行文档门禁，不执行无关构建和测试。

## 2026-07-17 - 编译与交叉编译工具链规划

### 变更内容

- 在部署说明中明确 IntelliSense、Clang Debug、GCC Release、ARM64 交叉构建和 Jetson 原生构建的职责。
- 记录当前交叉编译器与 CMake toolchain 已具备、目标 sysroot 和 ARM64 实机验证尚未完成。
- 补充宿主工具与目标依赖边界、版本匹配原则，以及 sysroot、交叉构建、打包和实机验收顺序。

### 设计决定

- 工具链规划归入部署说明，不新增重复专题文档；项目看板只保留硬件到位后的推进顺序。
- 在最终 JetPack/L4T 环境明确前，不用通用 ARM64 rootfs 代替 Jetson sysroot，也不宣称交叉构建可交付。

### 验证结果

- 核对 WSL 已安装 `aarch64-linux-gnu-g++` 11.4 和 rsync 3.2.7；Jetson sysroot 当前尚未准备。
- 文档格式和 pre-commit 检查通过。

## 2026-07-17 - 开发与发布编译器分工

### 变更内容

- `scripts/build.sh` 增加 `--compiler clang|gcc`，Debug 默认选择 Clang，Release 默认选择 GCC；Jetson
  交叉构建继续使用 GCC 工具链。
- VS Code IntelliSense 使用 x86_64 Debug 的 compile database 和系统 C++ 前端解析各 target 参数；实际
  Debug 构建仍由 `scripts/build.sh` 使用 Clang，避免编辑器配置参与正式工具链选择。
- CMake 记录编译器类型和版本；正式打包及 WSL matrix 校验 Release 为 GCC、开发检查为 Clang。

### 设计决定

- 编译器选择留在构建入口，不写入业务 target；构建目录仍保持 `_output/build/<架构>-<类型>`。
- Clang 用于更早暴露诊断和静态检查问题，GCC Release 对齐正式 Linux/Jetson 交付工具链。

### 验证结果

- Clang 14 Debug 全量 46/46 CTest、Navigator smoke、clang-tidy 和 pre-commit 通过。
- GCC 11.4 Release 构建、正式打包和 WSL matrix 通过；matrix 已校验两类构建的编译器身份。

## 2026-07-17 - 日志按时间切分与保留

### 变更内容

- `logging` 增加 `dump_time_secs`、`cut_off_time_mins` 和 `max_files`；默认后台每 5 秒刷盘、每 5 分钟
  切分，每个模块最多保留 10 个文件，删除旧的 `max_bytes` 大小切分配置。
- 日志按 `<模块>_YYYYMMDD_HHMMSS_mmm_序号.log` 命名；切分后按修改时间删除该模块最旧文件，升级后
  遗留的 `.log`/`.log.N` 也进入同一保留上限。
- module-child 不再预先创建错误命名的空 Navigator 日志；诊断快照改为从普通日志中选择最新 32 份。
- 新增 logger 测试，使用短测试周期覆盖缓冲刷盘、时间切分、文件命名和 10 文件保留，不等待真实五分钟。

### 设计决定

- 时间切分是唯一切分条件，不再因单文件字节数提前切分；空闲模块只推进切分期限，不持续制造空文件。
- `dump_time_secs` 由 logger 内部单一后台线程执行，线程、缓冲、文件和退出 flush 由同一个内部状态对象
  管理；公开接口仍只有 `InitLogger` 和 `Log`，业务模块不感知线程。
- 文件系统和配置属于不可信边界，保留字段类型、正数、普通文件和删除失败检查；日志调用内部不重复校验。

### 验证结果

- x86_64 Debug 全量 46/46 CTest、Navigator smoke、WSL matrix、clang-tidy 和 pre-commit 全部通过。

## 2026-07-16 - clang-tidy 阻断基线收口

### 变更内容

- 修复当前全量 `clang-tidy` 的 9 个 WarningsAsErrors：返回值移动、无效移动、无符号常量表达式、
  protobuf 枚举穷举和测试临时流生命周期。
- 未改变录包路径解析、1 MiB 行限制、64 KiB IPC 响应限制或 Runtime 生命周期行为。

### 设计决定

- 只修改告警所在表达式和边界分支，不新增 helper、类、文件或 CMake target。
- protobuf 枚举来自跨进程输入，未知数值统一映射为 `unknown` 和降级严重度；内部已知类型不重复防御。
- 本批只清除构建配置认定为错误的静态检查项，不为非阻断告警做无关重构。

### 验证结果

- 全量 `clang-tidy`、x86_64 Debug 45/45 CTest、Navigator smoke、WSL matrix 和 pre-commit 全部通过。

## 2026-07-16 - 有界诊断快照

### 变更内容

- `cockpit-ctl snapshot` 一次收集服务状态、Navigator Runtime 状态、构建信息和日志尾部；服务离线时
  仍生成快照并在清单中记录不可用原因。
- 复用 `cockpit-ctl status` 的 JSON 聚合结果，不重复实现健康查询；输出先写 `.partial` 目录，内容校验
  通过后原子发布。
- 日志只接受普通 `.log`/`.log.1` 文件，限制为 32 个且每个默认最多 256 KiB；不复制完整配置文件，
  避免未来将凭据和证书写入归档。
- 增加离线边界测试、Navigator 在线 smoke 和独立 WSL matrix 用例。

### 设计决定

- 当前缺口是故障发生后的证据归档，不是新的常驻控制面，因此不启用 `debugger` 动态模块，也不增加
  manager、数据库或第二套状态模型。只有远程触发、持续采样或独立生命周期需求明确后再实现它。
- 文件读写和共享状态查询属于不可信边界，保留集中校验；内部已验证 JSON 和类型值不重复防御。

### 验证结果

- x86_64 Debug 全量 45/45 CTest、Navigator smoke、包含 `diagnostic_snapshot` 的 WSL matrix 和
  pre-commit 全部通过。

## 2026-07-16 - Safe OTA 启动恢复与持久化顺序

### 变更内容

- 升级 YAML 状态使用临时文件 fsync、原子 rename 和父目录 fsync 发布；回滚、确认和事务清理同步对应
  目录，避免进程重启读取到半写状态。
- 安装脚本在发布 `current` 前同步候选内容，发布和手工回滚后再次同步安装根目录。
- Navigator systemd 模板在启动前执行 `safe-ota --recover`，自动收口开机或服务重启时遗留的事务。
- Safe OTA 测试补齐 candidate 未激活、current 已切换但仍为 prepared、activated 未确认、confirmed
  未清理四个恢复窗口；WSL matrix 增加独立 `ota_startup_recovery` 结果。

### 设计决定

- 持久化仍以 `upgrade-transaction.yaml` 为唯一事实，不增加数据库、state manager 或第二套恢复服务。
- 启动恢复复用同一个 safe-ota 可执行程序和文件锁；事务损坏时阻止 Navigator 启动，不猜测应运行版本。
- WSL 只验证落盘调用、状态恢复和启动配置，不把它等同于 Jetson 存储介质的真实拔电测试。

### 验证结果

- x86_64 Debug 全量 44/44 CTest、Navigator smoke、包含 `ota_startup_recovery` 的 WSL matrix 和
  pre-commit 全部通过。

## 2026-07-16 - 语音中断与 provider 异常恢复

### 变更内容

- VoiceInteractionControl 新增主动中断 RPC，取消当前 assistant/action，并清空尚未处理的 transcript；
  `voice-ctl --interrupt` 同时支持 text 和 JSON 输出。
- 交互服务使用 `features.ai.request_timeout_ms` 判定迟到 provider 结果，单次超时或异常恢复到 listening，
  不再把整个 Agent 永久置为 faulted。
- 状态新增请求中断、provider 超时和 provider 失败指标；连续 transcript 继续由现有有界队列串行处理。

### 设计决定

- 中断复用 `VoiceAssistant::Cancel()` 和已有 `ActionDispatcher::Cancel()`，没有新增 manager、线程池或
  timeout helper。真实网络 provider 必须自行使用同一超时预算设置 HTTP/gRPC deadline。
- 用户触发中断会丢弃当时排队的旧命令；中断之后提交的新命令仍可正常处理，不重启 Agent。

### 验证结果

- `voice_interaction_service_test` 覆盖三个连续命令有序响应、处理中断、两个排队命令丢弃、超时、
  provider 异常以及后续请求恢复。
- x86_64 Debug 全量 44/44 CTest、Navigator 单入口 smoke 和 pre-commit 全部通过。

## 2026-07-15 - WSL 配置、部署与故障矩阵

### 变更内容

- SystemConfig 对现有 schema 逐层拒绝未知字段，并携带完整 YAML 路径报告类型、范围和字段错误；修正
  `max_sessions/max_total_bytes` 误放在 gateway 下而被静默忽略的问题。
- 直接安装在写入 release 前校验 `SHA256SUMS`，回滚和安装拒绝非法版本名；健康检查根据 Navigator
  当前 mode 校验应运行模块和对应 gRPC 服务，不再使用永远返回成功的 status 命令。
- 新增 `run_wsl_matrix.sh`，以真实 Release 包在临时根目录执行双版本安装、配置保留、包篡改拒绝、
  normal mode 健康检查、故障注入和 rollback，并输出固定 schema 的 JSON 报告。

### 设计决定

- 配置 key 校验是外部 YAML 边界的共同规则，只保留一个复用函数；没有引入 schema 类、配置框架或
  每字段 helper。
- 故障矩阵直接组合已有 CTest 和真实 CLI/runtime，不复制 queue、shared memory 或 gRPC 测试逻辑。
- health 的服务集合由运行 mode 决定；normal 不要求 recording，development 要求 recording，cloud
  只要求 gateway，模块进程状态仍由 Navigator IPC 统一判定。

### 验证结果

- x86_64 Debug 全量 44/44 CTest 通过；Release 构建和 WSL 矩阵通过。
- JSON 报告六项全部 passed：配置边界、package/install/healthcheck/rollback、gateway 断流恢复、
  recording 不可达、队列满和 shared-memory writer 重启。

## 2026-07-14 - Qt HMI 相机动作闭环

### 变更内容

- 新增最小 `hmi.proto` 控制合同和 cockpit-ui `HmiControl`，通过本地 Unix-socket gRPC 接收受控动作。
- `open_camera_preview` 在 Qt 主线程切换 Camera 页面，只有页面状态更新后才返回成功；Agent 不再把
  本地记录当作执行成功。
- `play_music` 进入同一合同但在媒体播放器未接入时明确返回失败，VoiceResponse 保留真实动作结果。
- 通用 `paths` 增加 `run_dir`；车端默认为 `/cockpit-system/run`，WSL 随 `COCKPIT_RUNTIME_DIR`
  隔离本地 socket，不增加 HMI 业务配置字段。

### 设计决定

- 控制面继续使用 gRPC，不增加自定义 socket 文本协议；页面编号只存在于 Qt 内部，不暴露给 Agent。
- Qt 页面状态和 gRPC 生命周期由同一个 `HmiControl` 持有，没有拆分额外 model/server/helper 文件。
- 音乐播放器 owner 未确定前保持失败语义，不在 voice module 中提前实现媒体能力。

### 验证结果

- `hmi_control_test` 通过真实 gRPC 覆盖 Qt 主线程相机切页和媒体未实现失败；配置测试覆盖 `run_dir`。
- x86_64 Debug 全量 44/44 CTest、Navigator 完整 smoke 和 pre-commit 通过；smoke 验证 Agent、HMI
  module、cockpit-ui 与 voice-ctl 的跨进程动作结果。

## 2026-07-14 - 录包离线聚合报告

### 变更内容

- RecordingControl 增加 `GetReport`，一次返回 manifest/detail、合并时间线、损坏行和 artifact
  完整性，不增加数据库或第二套文件解析逻辑。
- `recording-ctl --report <session-id>` 支持 text/JSON，timeline 和 issue 分别限制为 1-1000 条；
  响应保留总 issue 数和截断状态。
- 报告整体健康要求 artifact 完整性通过且 JSONL 无损坏行；不健康时返回统一诊断退出码 3。

### 设计决定

- 聚合属于 recording 领域查询，直接组合现有 catalog、timeline 和 integrity 边界；没有新增报告
  helper 类、文件格式或状态数据库。
- gRPC 序列化复用 timeline/integrity 映射函数，避免 `GetTimeline`、`Verify` 和 `GetReport` 复制
  protobuf 字段转换。

### 验证结果

- `recording_service_test` 覆盖健康报告、损坏 JSONL、缺失文件、checksum 不匹配、issue 截断和非法
  limit；5 个 recording 测试及 x86_64 Debug 全量 43/43 CTest 通过。
- Navigator 完整 smoke 通过真实 recording module 和 gRPC 校验 JSON 报告字段；构建和 pre-commit
  通过。

## 2026-07-14 - 全代码实现状态复核

### 变更内容

- 按 CMake target 和业务域复核 core、Navigator、library、modules、drivers、apps、tools、tests、
  scripts、配置和 proto，并将代码事实回写到实现状态与项目看板。
- 明确车辆状态语音查询已经走 gateway gRPC；打开相机和播放音乐目前只记录 HMI handoff，不能算作
  实际执行闭环。
- 明确 carupload 仍是生命周期与占位发布日志，whisper.cpp 属于可选构建，TTS 当前是测试音实现。
- 重新排序 WSL 后续工作：录包离线报告、Qt 相机动作桥接、配置/打包/故障矩阵和 mock 语音异常矩阵。

### 设计决定

- 不新增状态文档和抽象层，只校准现有权威文档；“可构建”“可启动”“真实执行”使用不同完成口径。
- 不用代码量或目录数量推断完成度，以真实数据路径、外部副作用和自动化验证证据为准。

### 验证结果

- 代码审计覆盖当前 43 个标准 CTest、Navigator mode/IPC/dlopen ABI、各模块 Runtime、工具入口、
  package/install/rollback 脚本及主要异常测试；本批仅修改文档，格式检查结果见提交前验证。

## 2026-07-14 - 工具入口与构建收口

### 变更内容

- audio-probe、camera-ctl、camera-preview-probe、camera-probe、can-simulator、recording-ctl 和
  voice-ctl 的 `main.cc` 改为显式创建 `ProcessRuntime`、装配领域动作并统一记录结束。
- 删除只转发 `argc/argv` 的 `Run()` 入口，改用 `ProbeAudio`、`ControlCamera`、`ProbeCamera`、
  `SimulateCan`、`ControlRecording` 和 `ControlVoice` 等领域动作名。
- 删除业务文件中重复出现的 `Finish(runtime, result)` helper，所有普通退出分支只返回结果。
- 将九个仅服务对应二进制的 `*_commands` 静态库合并回 executable target，不改变源文件职责。

### 设计决定

- `topic` 和 `cockpit-ctl` 已由 main 直接解析命令并分发，保留现有代码结构，只收口 CMake target。
- 不为每个小工具机械创建 `action/common` 目录、options 类或通用 CLI framework；Safe OTA 的子目录
  来自真实升级职责，不是所有工具的固定模板。
- commands target 只在有第二个链接方、独立测试或依赖隔离价值时恢复，不用 target 数量表示
  模块化程度。

### 验证结果

- x86_64 Debug 构建、全量 43/43 CTest 和 Navigator 完整 smoke 通过；smoke 覆盖 CAN simulator、
  audio/camera probe、camera/recording/voice control 和 topic 命令。

## 2026-07-14 - Safe OTA 按职责分层

### 变更内容

- 参考 zelos safe_ota 的职责结构，`main.cc` 负责参数校验和启动装配，`common/` 定义稳定的
  升级选项，`action/` 负责进程互斥、Navigator 模式切换、健康确认和回滚编排。
- 删除根目录下的泛化 `Run()` 包装和单用途 `safe_ota_commands` 静态库；可执行程序仍使用一个
  CMake target。
- 升级请求、安装、恢复、回滚和确认继续由 `library/upgrader` 提供，Safe OTA action 只负责跨
  Runtime 编排。
- 在部署说明中补充后续演进条件，明确车辆状态、进程内状态表、多车型 factory 和远程接口
  的责任边界。

### 设计决定

- 模仿 zelos 的职责边界和命名方式，不复制尚无对应能力的 factory、state、table、restful 和
  examiner 层。
- 当前只有一种 Safe OTA action，不提前引入类继承和工厂；交易状态需被 Navigator upgrader 模块
  复用，因此依然属于 `library/upgrader`。

### 验证结果

- x86_64 Debug 构建、全量 43/43 CTest、Navigator 完整 smoke 和 pre-commit 通过；
  `safe_ota_test` 覆盖并发锁、中断恢复、健康失败回滚和真实 Unix socket 联动。

## 2026-07-14 - Safe OTA 进程中断恢复

### 变更内容

- 安装前原子写入 `run/upgrade-transaction.yaml`，记录 prepared、activated、confirmed 和上一 release。
- safe-ota 每次执行前自动恢复未确认事务，并提供 `--recover` 救援命令；恢复时同时还原 current、删除
  partial/未确认候选和事务文件。
- 增加 `safe-ota.lock` 非阻塞文件锁，拒绝同一安装根目录的并发升级或恢复。
- 健康失败、upgrader 超时、mode 恢复失败和确认写入失败统一走同一恢复入口。

### 设计决定

- 当前只解决 WSL 可验证的进程中断，不把普通文件 rename 误称为存储介质断电安全。
- 发布签名、CA 证书链、设备身份和后端发布授权留到后端协议明确后统一设计；SHA256 继续只表示内容
  完整性。
- 状态保留在一个 YAML 文件中，不增加升级数据库、daemon 或通用事务框架。

### 验证结果

- `safe_ota_test` 覆盖并发拒绝、prepared partial release、activated 未确认 release、健康失败和正常确认
  清理。
- x86_64 Debug 全量 43/43 CTest、Navigator 完整 smoke 和 pre-commit 通过。

## 2026-07-14 - Topic 运行状态诊断

### 变更内容

- Gateway `TopicMetadata` 增加 transport、预期更新周期、可用状态、最近更新年龄和错误原因。
- `/vehicle/state` 根据真实数据流返回 `waiting_for_data`、`available` 或 `stale`，CLI 不再硬编码
  transport。
- topic discovery 直接使用 protobuf metadata，删除重复的本地数据结构；完整 smoke 增加在线字段
  断言。

### 设计决定

- 自动化依赖 protobuf 可用状态枚举，错误原因保留为面向人的补充文本，避免脚本解析不稳定文案。
- 元数据生成集中服务 ListTopics/GetTopicInfo 两个 RPC；未为单个 topic 引入 registry 或额外配置层。

### 验证结果

- `gateway_grpc_service_test` 通过真实 Unix-socket gRPC 覆盖无数据、正常、过期和未知 topic。
- x86_64 Debug 全量 43/43 CTest 和 Navigator 完整 smoke 通过；在线 topic 字段断言通过。

## 2026-07-14 - WSL 输出目录统一收口

### 变更内容

- 新增统一 `_output/` 约定，使生成物在 VS Code 文件夹排序中位于源码目录之前；Debug/Release 构建
  写入 `_output/build`，打包暂存和归档写入 `_output/install`。
- WSL 脚本自动设置 `COCKPIT_RUNTIME_DIR`，将开发日志、数据、topic 文件和稳定性报告写入
  `_output/runtime`；业务 YAML 和车端 `/cockpit-system/{data,logs,run}` 目录保持不变。
- `.gitignore` 只忽略统一输出根目录，旧根级 build/install/logs/data 生成物完成迁移后不再隐藏。

### 设计决定

- 开发输出覆盖在 `SystemConfig` 外部环境边界集中应用，不生成第二份 WSL YAML，也不把开发机路径写进
  发布配置。
- `_output` 使用下划线前缀是为了在常见文件夹优先、名称排序中稳定显示在项目源码目录之前。

### 验证结果

- `system_config_test` 覆盖运行目录环境变量对 data/log/topic 路径的统一覆盖。
- `_output/build/x86_64-debug` 全新构建、42/42 CTest 和 Navigator 完整 smoke 通过；临时配置、
  Unix socket、日志及录包均位于 `_output/runtime/run`。
- `_output/build/x86_64-release` 构建和打包通过，归档生成于 `_output/install/dist`；旧根级输出目录已
  迁移或删除。

## 2026-07-14 - HMI 动态生命周期接入 Navigator

### 变更内容

- 将 `library/hmi` 从拒绝启动的 ABI 骨架替换为真实 `HmiRuntime`，负责启动、监管和停止同目录的
  `cockpit-ui`；UI 保持标准 Qt 主线程事件循环。
- UI 构建默认开启，normal/development mode 加入 hmi；headless 构建保持可选且不启动 hmi。
- 删除独立 `cockpit-ui.service`，安装脚本仅保留该历史 unit 的升级清理；`run_cockpit_ui.sh` 不再重复
  启动 UI，而是等待 Navigator 统一管理。
- 新增 offscreen 生命周期测试：杀死 UI 后验证 HMI module 被重启，杀死 HMI module child 后验证旧
  UI 收到 parent-death signal 退出，并由 Navigator 重建完整 HMI。

### 设计决定

- module ABI 的 `poll()` 用于报告生命周期状态，不承担 Qt event loop；HMI module child 监管独立 UI
  进程，使 Qt 主线程约束和 Navigator 模块管理同时成立。
- 进程创建和回收逻辑只服务 HMI 资源边界，直接留在 `HmiRuntime`，不新增通用 process helper。

### 验证结果

- `hmi_runtime_test` 在 Qt offscreen 环境通过，覆盖 UI 和 HMI module 两个方向的崩溃恢复。
- x86_64 Debug 全量 42/42 CTest、包含 hmi 的 Navigator 完整 smoke 和 UI 入口受控退出通过；UI-enabled
  Release 构建与打包通过，包内只保留 Navigator systemd service。显式 headless mode 配置测试通过。

## 2026-07-13 - Navigator 单入口验证与稳定性基线

### 变更内容

- 新增 `run_navigator_stability.sh`，支持 normal/development、采样时长和间隔、restart/crash 注入及
  自定义报告路径；运行数据和日志统一写到 build 目录。
- 每次采样记录 mode、全部模块的 state/PID/exit/restart count，以及五个 gRPC 服务的结构化 health；
  报告携带版本、Git revision、配置 SHA256、故障前后 PID 和汇总结果。
- 新增 3 秒短 CI：真实启动 development 组合，杀死 camera driver，验证 Navigator 自动换 PID 拉起，
  后续模块状态和 gRPC health 持续正常。
- 将完整 smoke、Qt UI 联调和 vcan smoke 改为只启动一个 Navigator，由 mode 选择真实模块组合；保留
  camera、audio、recording、topic、voice 和 SocketCAN 的原有断言。
- 删除七个独立 service 薄入口、对应 CMake/安装目标和占位 README；相关测试移到 `tests/library` 并
  直接链接真实 library target。安装脚本只保留旧 systemd unit 的升级清理名单。
- 删除未实现的 `apps/web-dashboard` 占位 README，并将运行时 `/data/` 加入 `.gitignore`。

### 设计决定

- 稳定性编排保持为一个脚本，不新增 reporter 类、辅助工具目录或数据库；固定控制协议在边界处一次
  转换为 JSON，业务模块不感知诊断过程。
- WSL 短 CI 只证明进程管理、健康采样和故障恢复闭环，不代替 Jetson 硬件、systemd 或长时间实机结论。

### 验证结果

- development crash 注入后 camera driver restart count 变为 1，四次采样全部健康；normal restart
  注入和对应 mode 健康判定通过，两份报告均通过 JSON 解析。
- x86_64 Debug 全量 41/41 CTest、Navigator 单入口完整 smoke 和 pre-commit 通过。

## 2026-07-13 - Upgrader 与 Safe OTA 开发机闭环

### 变更内容

- 将 `library/upgrader` 从拒绝启动的 ABI 骨架替换为真实 Runtime；新增独占 `upgrade` mode，通过
  `run/upgrade-request.yaml` 和 `run/upgrade-result.yaml` 保存升级请求与结果。
- 新增一次性 `safe-ota`：要求显式确认候选版本，校验发布包 SHA256 清单，调用包内安装器写入独立
  release 目录并原子切换 `current`，切回 normal 后执行健康检查。
- 健康检查或 normal 模式恢复失败时还原旧 symlink、删除失败 release，并通过 Navigator `reload`
  重新加载旧模块；安装器异常退出时同步清理半成品目录。
- Navigator 和 `cockpit-ctl runtime` 增加 `reload`，测试覆盖当前 mode 的整组模块重新加载。
- 新增 `safe_ota_test`，使用临时安装根目录覆盖独立安装、健康失败回滚、篡改包拒绝，以及真实
  Navigator 的 `normal -> upgrade -> normal` 事务。

### 设计决定

- upgrader 负责候选包安装，safe-ota 负责人工确认、模式切换、健康确认和回滚编排；业务配置不暴露
  动态库或升级编排规则。
- 当前只实现 WSL 可自动验证的版本目录事务，不把 SHA256 误称为发布签名，也不提前宣称具备 A/B
  分区、断电恢复、systemd unit 更新或量产安全能力。

### 验证结果

- x86_64 Debug 构建、全量 40/40 CTest 和原完整 smoke 通过。
- x86_64 Release 构建和发布包生成通过；包内 `safe-ota`、`libupgrader.so` 存在且整包 SHA256 清单
  校验通过。

## 2026-07-13 - Navigator 真实模块迁移与单入口切换

### 变更内容

- 删除 `configs/navigator.yaml`；参考 znavigator，将模块到动态库的映射和 normal/development/cloud
  组合集中到 `navigator/run_config`，所有 module child 只接收统一的 `config.yaml`。
- 将 gateway、vehicle、audio、camera、voice interaction、recording 和 cloud uplink 的真实业务装配
  迁入 `library/transfer`、`library/driver/*`、`library/agent`、`library/recording` 和
  `library/carupload`；原 `processes/*/main.cc` 改为复用同一 Runtime 的薄兼容入口。
- 模块 ABI 增加 `poll` 生命周期检查并升级到 v2；worker 意外退出会传递给 Navigator，再由父进程按
  重启窗口处理。修正启动入口未应用 `--socket` 的问题，并检查 readiness pipe 写入结果。
- `cockpit-ctl` 新增 runtime status/mode/switch/start/stop/restart 控制命令及集成测试。
- normal 只启动 transfer、三类 driver 和 agent；development 增加 recording；cloud 启动 transfer、
  vehicle_driver 和 carupload。HMI、upgrader、debugger、calibration、watchdog 保留 ABI 骨架，但不进入
  mode 且显式启动失败，避免空模块显示为健康。
- 三个 systemd target 改为互斥地托管 `cockpit-navigator@<mode>.service`，删除旧 backend service
  unit。安装脚本会停用并清理旧 unit，Qt UI 保持独立应用入口。
- 打包暂存和归档输出从仓库根目录的 `stage/dist` 收口到 `install/stage` 和 `install/dist`，发布包不再
  携带 Navigator 业务编排 YAML。

### 设计决定

- 模块清单和 mode 是产品运行策略，不是现场业务参数；当前由代码维护，防止动态库路径和任意模块组合
  直接暴露到业务配置。
- systemd 只负责 Navigator 的进程存活和启动 mode；模块启停、增量模式切换、状态、崩溃重启和退出
  回收统一由 Navigator 管理。
- 不把尚未实现的新能力包装成空 Runtime。HMI 需先解决 Qt 主线程事件循环；升级、安全 OTA、标定和
  硬件 watchdog 按真实边界实现后再加入运行组合。

### 验证结果

- x86_64 Debug 全量 39/39 CTest 通过，原完整 smoke 通过。
- 真实 Navigator normal 模式下 transfer、vehicle/audio/camera driver 和 agent 均进入 running；
  切换 development 后 recording 增量启动，统一 shutdown 正常回收。
- x86_64 Release 构建和发布包生成通过；产物位于 `install/`，包内只包含 Navigator 模板 service、
  三个 mode target 和 Qt UI unit，临时根目录安装验证通过。

## 2026-07-12 - Navigator 动态模块运行时第一批

### 变更内容

- 参考 znavigator 的职责组织新增 `cockpit/navigator`，保留 `connection`、`dl_api`、`run_config`、
  `process` 和 `common` 子目录，但只维护一个 CMake target 入口。
- 新增 `cockpit-navigator`：父进程按配置启动同一二进制的 module child，子进程通过版本化 C ABI、
  `dlopen` 和 `dlsym` 加载业务动态库。
- 实现 normal/development/upgrade 模式、模块 start/stop/restart、状态查询、Unix Socket 控制、退出
  回收和时间窗口内的崩溃重启限制。
- 新增 transfer、driver、agent、hmi、carupload、recording、upgrader、debugger、calibration 和
  watchdog 可加载骨架；骨架只有 ABI 入口，不预建空业务类。
- 发布包增加 Navigator 二进制、动态模块、配置模板和 systemd 单元；旧业务进程与 target 暂不切换。

### 设计决定

- systemd 最终只守护 Navigator，模块进程由 Navigator 统一管理；迁移期间旧进程继续作为真实业务
  链路，防止骨架模块被误认为已经接管硬件。
- Navigator 本地控制接口属于运行时核心，transfer 负责后续外部 IPC/REST，不形成自管理环。
- `library/*` 是进程级动态模块，`modules/*` 是进程内复用实现，二者不再表达同一层职责。

### 验证结果

- x86_64 Debug 编译通过；Navigator 集成测试覆盖模式切换、显式重启、崩溃达到重启上限、干净退出
  和 ABI 不兼容拒绝。
- 配置测试覆盖相对路径解析和 mode 引用未知模块拒绝；x86_64 Debug 全量 39 项 CTest、原完整 smoke
  和 pre-commit 全部通过。
- Debug 发布包和临时根目录安装通过，包内包含 Navigator、12 个动态模块、配置模板和 systemd 单元；
  临时安装产物的最终 Socket 启动验证因执行授权额度被环境拒绝，仓库构建产物的同场景已通过。
- 最后补充 Socket 普通文件保护、客户端读超时和断连 `SIGPIPE` 防护后，增量编译与 pre-commit
  通过；再次执行 Socket 测试同样因授权额度被环境拒绝，未通过替换命令绕过。

## 2026-07-12 - 仓库结构与代码边界收口

### 变更内容

- 将 core、audio、camera、voice、UI 和三个 service 的叶子级 CMake 合并到领域入口，cockpit 产品
  CMake 文件从 63 个收口为 22 个，target 名称和链接粒度保持不变。
- `Logger`、`Time`、`Args`、`Module`、`ProcessRuntime` 和 `VehicleState` 等历史文件统一改为
  `snake_case`，同步源码、测试、构建和文档引用。
- recording event publisher 从原 `services/recording-service/client` 迁移到
  `modules/recording/client`，camera 和 voice 不再引用另一个 service 的内部目录。
- `cockpit-ctl` 恢复单目录平铺；audio-probe、camera-ctl、voice-ctl 和 recording-ctl 参考 topic，
  将入口与命令实现分离为 executable 和 commands target。
- camera probe、preview probe 和 CAN simulator 同步采用薄入口和 commands target；实现文件统一
  使用 `audio_probe.cc`、`camera_ctl.cc` 等 CLI 对应名，所有工具保持单目录平铺。
- tests 按 core、modules、processes、apps、tools 和 integration 分组，继续由一个 CMake 文件集中声明。
- deploy 源文件迁入 `deploy`，发布包内部目录和安装合同保持不变。
- 将长期蓝图、WSL 路线、范围策略和逐文件参考审计的独有决策迁入四份权威文档后删除重复文件，
  文档入口改为职责表。
- 内联五个只包装 hardcode 或函数内部值的 helper；代码风格明确“边界需要校验”不等于“校验需要
  helper”，并区分可信值和不可信边界。
- 产品源码入口从 `cockpit/services` 迁移到 `cockpit/processes`，`ServiceRuntime` 改为
  `ProcessRuntime`；二进制名、systemd `.service` 和 protobuf Service 契约保持兼容。
- `cockpit.target` 只包含核心产品进程；新增 development/cloud target 分别启用 recording 和
  cloud uplink，camera/voice 不再依赖研发录包进程。
- 删除泛化 `core/utils`，主机时间能力迁入 `core/time` 和 `time_util` target。

### 设计决定

- 业务子目录可以保留，但少量文件不再各自维护 CMake；target 只服务真实复用、依赖和条件编译边界。
- 多命令工具保持一个目录并使用描述明确的命令 target，不建立单命令子目录或通用 helper 层。
- 本轮不按行数拆业务类和函数，不增加 manager/factory；结构调整不改变运行行为和 proto 契约。
- 用户输入、配置/文件、外部 API、数据库和跨进程/团队接口必须校验；hardcode、函数内部值和类型
  系统已保证的值不增加重复防御。
- systemd target 是生产 launch profile，`ProcessRuntime` 管单进程，`ModuleManager` 管真实生命周期
  模块；不增加与 systemd 重复的 launcher，也不把同步业务类机械包装成 Module。

### 验证结果

- x86_64 Debug 全量构建、37 项 CTest、完整 smoke 和 pre-commit 通过；沙箱内 smoke 因禁止创建
  本地 socket 失败，允许本地 gRPC socket 后完整通过。
- Debug 发布包生成通过，包内包含 install、healthcheck、rollback 和第三方许可证文件。
- 完整 smoke 前两次在 recording VehicleState stream 启动波动处失败，第三次完整通过；该既有时序
  风险没有通过增加无界重试掩盖，后续长稳批次继续统计。

## 2026-07-12 - WSL 路线重排与健康变化历史

### 变更内容

- 重新审计 cockpit-system 全部源码/测试，并复查 znavigator、zcarcloud 的配置、生命周期、测试和
  transport 边界，新增 `WSL开发路线图.md`。
- `ServiceHealthModel` 保留最近 32 条状态切换，并为每个服务记录最近一次 degraded/faulted 的时间
  和原因；初次采样只建立基线。
- Dashboard 服务行显示最近问题，新增 Diagnostics 标签页展示状态切换、原因和时间。
- 新增 `service_health_model_test`，覆盖 degraded、恢复、原因保留和有界淘汰。

### 设计决定

- 历史只属于 cockpit-ui 当前进程，不增加 manager、数据库或新 service；UI 重启后清空。
- WSL 后续依次推进 topic 诊断、长稳报告、录包报告和配置/故障矩阵；真实硬件参数继续等待实机。

### 验证结果

- x86_64 Debug 构建、全量 37 项 CTest、完整 smoke 和 pre-commit 通过。
- `run_cockpit_ui.sh --offscreen` 成功加载新增 Diagnostics 页面，无 QML 属性或类型错误；限时验证由
  timeout 正常结束。

## 2026-07-11 - 克制抽象与薄辅助函数清理

### 变更内容

- `代码风格.md` 增加克制抽象标准，明确辅助函数、类和接口的增加条件及 review 顺序。
- `模块化策略.md` 明确模块化不等于拆分每段短逻辑，单次简单逻辑默认留在调用处。
- 内联 camera 设备可用条件、默认照片名和录包时间范围判断，删除三个单次薄辅助函数及声明。

### 设计决定

- 不以函数行数或调用次数作为绝对规则；复杂算法、资源、线程和协议边界即使单次调用也可以保留。
- 本轮不批量删除现有 helper，只处理确定减少符号和阅读跳转、且不改变所有权与行为的低风险案例。

### 验证结果

- camera、recording 定向测试、全量 36 项 CTest 和 pre-commit 验证通过。
- smoke 首次在既有 topic `hz` 链路只收到 2/3 条后超时，复跑完整通过，改动相关的 camera 和
  recording 链路两次均通过。

## 2026-07-11 - 录包完整性批量汇总

### 变更内容

- recording-service 新增按会话开始时间和 limit 批量校验接口，汇总 healthy、damaged、unavailable。
- `recording-ctl --verify-all` 支持 text/JSON 输出；存在损坏或不可用会话时返回退出码 3。
- 单个损坏 manifest 被隔离为 unavailable，其余会话继续校验；单会话 `--verify` 继续提供完整 issue。

### 设计决定

- `total_sessions` 表示时间过滤后的会话总数，各状态计数只覆盖 limit 内实际检查的会话。
- 批量 RPC 只返回筛选摘要，避免 issue 数量无界；原始 JSONL 和 manifest 继续作为权威来源，不引入
  SQLite 或新的持久化汇总文件。

### 验证结果

- recording-service 测试覆盖健康、损坏、不可用、空目录、时间过滤、limit 截断和非法范围。
- smoke 通过真实 gRPC 调用校验批量 JSON 汇总；全量 CTest 和 pre-commit 验证通过。

## 2026-07-11 - 诊断 CLI 结构化输出与退出码

### 变更内容

- 新增 `tools/diagnostics`，统一 `--output text|json` 解析、protobuf JSON 输出、错误 JSON 和退出码。
- camera-ctl、voice-ctl、audio-probe 的 gRPC 控制面状态支持 JSON；流式 voice/audio 事件使用
  JSON Lines。
- `cockpit-ctl status/health` 支持聚合 JSON，离线服务保留错误对象；health 不通过改为退出码 3。
- camera-ctl 增加互斥命令和正数尺寸校验，参数错误不再发往服务端。
- recording-ctl 控制面 deadline 从 2 秒放宽到 5 秒，降低 WSL 高负载 smoke 的误超时。

### 设计决定

- 退出码固定为 0 成功、1 操作/RPC 失败、2 参数错误、3 健康状态不通过。
- JSON 直接使用 protobuf 官方转换和 proto 字段名；本地 ALSA/WAV 操作不承诺结构化合同。

### 验证结果

- 新增 `cli_output_test`，覆盖格式解析、JSON 合法性、64 位字段和退出码。
- smoke 覆盖在线 camera、audio、voice JSON 状态；离线 cockpit health JSON 返回退出码 3。

## 2026-07-11 - camera 合成故障注入与运行期看门狗

### 变更内容

- 新增 `SyntheticPreviewSource`，产生 BGRx 测试帧并支持 no-frames、stall、disconnect 故障。
- camera-service 增加运行期 preview 看门狗，将无首帧、帧流卡住和后端断开分别记录为
  `no_frames`、`frame_stalled`、`source_disconnected`。
- camera 配置新增 capture backend、卡帧阈值和合成故障参数；默认仍使用 GStreamer。
- 新增 `camera_synthetic_stability_test` 和可重复执行的 `run_camera_stability.sh`。

### 设计决定

- 合成源实现现有 `CameraPreviewSource`，帧仍经过 CameraService、sink 和正式状态机，不建立测试专用
  旁路。
- 看门狗只负责确认并停止故障源；恢复继续由显式 start 触发，以保持 restart 和 recover 含义稳定。

### 验证结果

- 定向测试覆盖三类故障、恢复清错、recover 计数和运行中 restart 计数。
- 20 轮合成稳定性脚本、全量 CTest、smoke 和 pre-commit 验证通过。

## 2026-07-11 - 录包文件完整性诊断

### 变更内容

- 新增 `RecordingIntegrityVerifier`，校验 data file 索引格式、会话内路径边界、存在性、普通文件
  类型、大小和 checksum，并一次返回全部问题。
- recording-service 新增 `Verify` gRPC 接口，`recording-ctl --verify` 输出汇总和逐项 issue；发现
  完整性问题时返回退出码 2。
- `copy_into_session` 在调用方未提供 checksum 时自动生成 `fnv1a64`，写入索引供后续复盘校验。

### 设计决定

- 会话内 artifact 禁止绝对路径、`..` 和符号链接逃逸；外部文件索引仍允许指向会话外路径。
- 当前只内建 `fnv1a64` 校验，空 checksum 和其他算法计入 unavailable，不误报为 checksum 错误；
  后续确有互操作需求时再引入 SHA-256 实现。

### 验证结果

- 新增完整性测试，覆盖正常文件、损坏索引、路径逃逸、缺失文件、非普通文件、大小和 checksum
  不匹配；recording-service 测试覆盖缺失外部文件。
- smoke 使用真实会话内 artifact，并通过 gRPC 验证文件和自动生成的 checksum。

## 2026-07-11 - 服务健康语义一致性收口

### 变更内容

- `ServiceHealthState` 新增 `DISABLED` 和 `UNKNOWN`，协议中的 `UNSPECIFIED` 统一按 unknown 解释。
- 新增 `core/health`，集中提供状态名称、严重度和脚本化 health check 判定。
- audio capture、camera preview 停止和 voice interaction 主动关闭改为 `DISABLED`，不再误报
  `DEGRADED`。
- Qt Dashboard 增加 disabled 汇总，并将不可达 unknown 的严重度置于 degraded 之上；
  `cockpit-ctl status/health` 使用同一套解释规则。

### 设计决定

- `DISABLED` 表示明确的主动关闭，`DEGRADED` 表示仍可用但核心能力受损，`UNKNOWN` 表示无法获得
  可信状态，`FAULTED` 表示已确认故障。
- health check 接受 `OK`、`DISABLED` 和 `DEGRADED`；`UNKNOWN` 与 `FAULTED` 返回失败。

### 验证结果

- 新增 `service_health_test`，覆盖名称规范化、严重度顺序和 health check 判定。
- 全量构建、CTest、smoke 和 pre-commit 验证通过。

## 2026-07-11 - 录包多源时间线查询

### 变更内容

- 新增 `RecordingTimelineReader`，读取 VehicleState、events 和 data files JSONL，按主机侧
  `timestamp_ms` 稳定归并，支持闭区间过滤、limit、截断标记和损坏行计数。
- recording-service 新增 `GetTimeline` gRPC 接口，`recording-ctl --timeline` 支持命令行查询。
- 架构文档补充当前时间字段和真实硬件接入后可扩展的 clock domain、source/host timestamp、
  offset 与 uncertainty。

### 设计决定

- 多源时间线归并不等同于多时钟源同步；当前单机原型不提前实现 PTP、设备时钟校准或漂移估计。
- ECU timestamp、camera PTS 和 audio sample clock 实际接入后，再依据设备能力设计时间戳归一化。

### 验证结果

- 新增 reader 范围、limit、稳定排序和损坏行测试，并扩展 recording-service 集成测试。
- smoke 覆盖 VehicleState、event 和 data file 三类条目的 gRPC 合并查询。

## 2026-07-11 - 全量代码 review 修复

### 变更内容

- camera shared memory 支持异常退出后的遗留对象回收，帧槽改用 robust process-shared mutex；
  capture、shared memory reader 和 Qt client 增加格式、stride、payload 大小及整数边界校验。
- 新增 `core/json`，统一 JSON value 校验和 UTF-8/控制字符转义，录包不再接受非法 raw JSON。
- recording 未激活时明确拒绝 event/data file；停止和异常恢复检查 JSONL、manifest 及 marker
  的 flush/close，`COMPLETE`/`INTERRUPTED` 在目录发布前写入。
- `AppendDataFile` 增加 `copy_into_session`，camera 拍照文件复制到会话 `artifacts/`，纳入目录大小、
  retention 和删除范围。
- camera/voice 到 recording 改为有界后台队列，录包 RPC 不再阻塞拍照响应或语音处理锁。
- voice 停止时丢弃待处理 transcript，并取消 gateway RPC 和重试等待；ALSA playback 改为非阻塞写入，
  SpeechOutput 的停止信号贯通 write/drain。
- 配置层固定校验 16 kHz、mono、20 ms 音频契约；audio processing 和 speech output 改为链接直接
  voice 子模块，移除对聚合 `voice` target 的不必要耦合。

### 设计决定

- gRPC 继续只传录包控制、事件和文件描述；相机像素不进入 gRPC，照片本体由 recording-service
  从本地文件复制。
- 录包发布接口的返回值表示是否进入本地队列；录包未启动属于预期弱依赖状态，其他远端错误记录 warning。
- 停止流程优先保证有界退出，队列中未执行的语音和播放任务计入 dropped，不在 shutdown 阶段排空。

### 验证结果

- x86_64 Debug 全量构建通过，CTest 31/31。
- `bash scripts/tests/smoke.sh` 通过，覆盖 recording、topic、audio、voice、camera 和 cloud placeholder。
- `pre-commit run --all-files` 和手动阶段 `clang-tidy` 全量检查通过。

## 2026-07-08 - camera 稳定性、gateway health 与录包多源索引

### 变更内容

- gateway proto/gRPC 新增 `GetStatus`，返回统一 `ServiceHealth`、车辆状态新鲜度和事件计数。
- cockpit-ui 健康总览和 `cockpit-ctl status/health` 改用 gateway 原生 health，不再通过
  `GetLatestVehicleState` 推断。
- camera status 新增 `RECOVERING`、`last_error_kind`、`restart_count`、`recover_count` 和
  `last_recover_at_ms`。
- `camera-ctl` 与 `cockpit-ctl status` 输出 camera 恢复计数和错误分类。
- camera MessageBus 到 recording 的转发采样策略抽为 `CameraRecordingBridgeFilter`，并补单测覆盖。
- recording 新增 `data_files.jsonl`，proto/gRPC 新增 `AppendDataFile`，`recording-ctl` 新增
  `--file-path` 索引入口。
- smoke 在录制会话中追加 camera 文件索引，并通过 `recording-ctl --detail` 校验索引计数。

### 设计决定

- gateway 的健康语义与 `GetLatestVehicleState` 共用同一个车辆状态新鲜度阈值，避免 CLI、UI 和
  RPC 判断不一致。
- camera 错误分类使用轻量字符串 `last_error_kind`，先覆盖 invalid argument、device unavailable、
  backend unavailable 和 start failed 等原型阶段常见原因。
- `data_files.jsonl` 只记录大块数据文件索引，不保存图片、音频或视频二进制；`events.jsonl`
  继续只承载轻量事件元数据。

### 验证结果

- `bash scripts/build.sh` 通过，CTest 29/29。
- `bash scripts/tests/smoke.sh` 通过，覆盖 gateway 原生 health、camera status 和 recording
  `data_files_indexed: 1`。
- `pre-commit run --files ...` 通过。

## 2026-07-08 - 录包可复盘性增强

### 变更内容

- 新增 `core/build`，由 CMake 注入 project version、build type、git commit 和 dirty 状态。
- recording manifest 增加 `config_checksum`、`git_commit`、`git_dirty`、`build_type` 和
  `binary_version`。
- recording catalog 新增 session detail 读取能力，从 `manifest.json` 重建单会话复盘信息。
- recording proto/gRPC 新增 `GetDetail`，`recording-ctl` 新增 `--detail <session-id>`。
- smoke 增加 `recording-ctl --detail` 校验，覆盖 config checksum、git commit 和 binary version。
- `项目进度总览.md` 将录包可复盘性移入已完成主线，并把下一步调整为 camera 稳定性、
  gateway 原生健康接口和录包多源索引。

### 设计决定

- 仍保持 manifest 文件为权威来源，不引入 SQLite 运行依赖。
- config checksum 使用稳定的 FNV-1a 64 位十六进制指纹，避免为 WSL 原型阶段额外引入 crypto
  依赖。
- `events.jsonl` 继续只保存轻量元数据，大块数据后续通过独立文件索引接入。

### 验证结果

- `bash scripts/build.sh` 通过，CTest 29/29。

## 2026-07-08 - UI 健康总览

### 变更内容

- cockpit-ui 新增 `ServiceHealthModel`，后台定时轮询 gateway、audio、voice、camera 和 recording。
- Qt Dashboard 右侧新增服务健康列表，展示 OK、DEGRADED、FAULTED 和 UNKNOWN 状态、摘要和错误。
- `cockpit-ui` 启动时从 `configs/config.yaml` 读取各服务 gRPC 地址，并把健康模型注入 QML。
- `项目进度总览.md` 将 UI 健康总览移入已完成主线，并把下一步调整为录包可复盘、camera 稳定性和 gateway 原生健康接口。
- `实现状态.md` 增加 UI 健康总览模块状态。

### 设计决定

- UI 健康页只走 gRPC 控制面 `GetStatus` 或轻量状态查询，不承载 PCM、图像或其他大数据。
- audio、voice、camera 和 recording 直接使用 proto 中已有 `ServiceHealth`。
- gateway 暂未提供统一 health RPC，UI 当前通过 `GetLatestVehicleState` 可达性和数据新鲜度推断健康。
- 服务不可达显示 UNKNOWN，避免 WSL 阶段只启动部分服务时误报 fault。

### 验证结果

- `bash scripts/build.sh` 通过，CTest 29/29。
- `bash scripts/tests/smoke.sh` 通过。
- `timeout 6s bash scripts/run-cockpit-ui.sh --offscreen` 在提权允许本地 gRPC 绑定后成功加载 cockpit-ui，并由 timeout 结束。

## 2026-07-08 - 文档职责收口

### 变更内容

- `实现状态.md` 改为模块状态表，集中记录状态、入口、验证方式和下一步。
- `项目进度总览.md` 改为日常推进看板，只保留当前阶段、已完成主线、下一步 3 件事和风险。
- `architecture.md` 与 `architecture_refined_v0.4.md` 补充文档定位，避免当前架构、长期蓝图和实现状态互相重复。
- `docs/文档导航.md` 调整导航顺序，优先展示当前架构、实现状态和项目看板。

### 设计决定

- 不删除专题文档；通过职责分工减少重复维护。
- 模块完成度只放 `实现状态.md`，日常计划只放 `项目进度总览.md`，每批改动只放 `变更记录.md`。

### 验证结果

- `pre-commit run --files docs/实现状态.md docs/项目进度总览.md docs/系统架构.md docs/architecture_refined_v0.4.md docs/文档导航.md docs/变更记录.md` 通过。
- `git diff --check` 通过。

## 2026-07-08 - 文档中文文件名

### 变更内容

- 将 docs 下常用项目文档改为中文文件名，方便日常查看和讨论。
- 补充将模块化策略、项目范围与仓库策略两个文档改为中文文件名。
- 同步更新 `README.md` 和 `docs/文档导航.md` 中的文档链接。
- 保留 `README.md`、`architecture.md`、`architecture_refined_v0.4.md` 等入口和版本文档的英文命名。

### 设计决定

- 文档可以使用中文文件名；代码、脚本、配置、proto 和 CMake 仍保持英文路径。
- 中文文件名只用于面向人阅读的说明文档，不扩散到构建系统和源码 include 路径。

### 验证结果

- `pre-commit run --files README.md docs/文档导航.md docs/项目进度总览.md docs/实现状态.md docs/配置说明.md docs/部署说明.md docs/代码风格.md docs/运行时通信策略.md docs/参考项目.md docs/参考代码审计.md docs/语音AI规划.md docs/变更记录.md` 通过。
- `git diff --check` 通过。

## 2026-07-08 - 参考 zelos 收口代码风格

### 变更内容

- 新增 `CPPLINT.cfg`，核心规则参考 zelos/znavigator/zcarcloud：100 列、`#pragma once`、不过度要求
  include what you use。
- 新增 `docs/代码风格.md`，明确当前项目参考 zelos 的 C++ 风格边界。
- 新增 `cockpit/core/base/macros.h`，提供 `COCKPIT_DISALLOW_COPY_AND_ASSIGN`，统一不可拷贝类写法。
- core event/runtime、recording session、camera service 等近期活跃代码改用统一禁拷贝宏。
- 全仓成对禁拷贝声明统一为 `COCKPIT_DISALLOW_COPY_AND_ASSIGN`；保留 `AudioFrame` 这类仅禁赋值的特殊语义。

### 设计决定

- 不照搬 zelos 内部版权头、私有 pre-commit 源、zlog/zutil 依赖和历史 Bazel 配置。
- 当前环境未默认安装 cpplint，因此先保留配置文件，不加入普通 commit 强制门禁。
- 不做全仓文件名和类名大重命名，避免风格整理影响功能开发节奏。

### 验证结果

- x86_64 Debug 构建通过，CTest 29/29。
- `pre-commit run --all-files` 通过。

## 2026-07-08 - 项目进度总览文档

### 变更内容

- 新增 `docs/项目进度总览.md`，集中整理已完成、未完成和准备完成的任务。
- `docs/文档导航.md` 增加项目进度看板入口。

### 设计决定

- `项目进度总览.md` 面向日常推进和决策；`实现状态.md` 继续保留更细的实现状态。
- 硬件相关任务单独归到有 Jetson/外设后的阶段，避免 WSL 阶段被真实设备阻塞。

### 验证结果

- `pre-commit run --files docs/项目进度总览.md docs/文档导航.md docs/变更记录.md` 通过。

## 2026-07-07 - Runtime 依赖图、camera MessageBus 接入与录包元数据

### 变更内容

- `core/runtime` 新增 `DependencyGraph`，支持 required/optional 服务依赖和 required 环检测。
- `configs/config.yaml` 新增 `runtime.dependencies`，`cockpit-ctl dependencies` 可打印当前服务依赖图。
- camera-service 接入进程内 `MessageBus`，发布 `/camera/status` 和 `/camera/frame_meta`。
- camera-service 增加 recording bridge，采样转发 frame metadata 和状态事件到 recording-service。
- camera status 增加 preview 启动时间、连续 drop、最大连续 drop、连续 source gap 等可用性指标。
- recording manifest 增加 project、schema_version、config_path 和 sources 元数据。

### 设计决定

- `runtime.dependencies` 只表达依赖关系，不负责启动进程；进程生命周期仍由 systemd/脚本管理。
- camera 帧像素继续走 shared memory，MessageBus 和 recording 只承载轻量元数据。
- frame metadata 转发给 recording-service 做采样，避免按帧率打满 gRPC 控制面。

### 验证结果

- x86_64 Debug 构建通过，CTest 29/29。
- 完整 `scripts/tests/smoke.sh` 通过；第一次运行遇到一次 audio status RPC 超时，重跑通过，服务日志无 fault。
- `cockpit-ctl dependencies --config configs/config.yaml` 输出 required/optional 依赖图正常。
- `pre-commit run --all-files` 通过；`git diff --check` 无空白错误。

## 2026-07-07 - Runtime MessageBus、统一健康模型与多源录包事件

### 变更内容

- `core/event` 新增进程内 `MessageBus`，支持 topic 订阅、`*` 通配订阅、队列容量、drop 统计和 metrics。
- `common.proto` 新增 `ServiceHealth`，audio、camera、voice 和 recording status 均携带统一健康字段。
- recording gRPC 新增 `AppendEvent`；`recording-ctl` 新增 `--event-topic/--event-payload` 调试入口。
- camera 拍照成功后写入 `/camera/photo` 元数据事件；voice response 写入 `/voice/response` 元数据事件。
- systemd 服务补充 vehicle/gateway/audio/voice/camera/recording 依赖关系，新增 `scripts/check-health.sh`。

### 设计决定

- `MessageBus` 只用于进程内小消息，不替代 gRPC、shared memory 或 SPSC RingBuffer。
- 录包事件只保存元数据和小 JSON，不保存图片、音频、视频二进制。
- camera/voice 对 recording-service 是弱依赖：录包服务不可用时只记录 warning，不影响用户主流程。

### 验证结果

- x86_64 Debug 构建通过，CTest 28/28。
- 完整 `scripts/tests/smoke.sh` 通过，覆盖 recording `AppendEvent`、`events.jsonl` 落盘和现有服务链路。
- `pre-commit run --all-files` 通过；`git diff --check` 无空白错误。

## 2026-07-07 - cockpit-ctl 健康检查

### 变更内容

- `cockpit-ctl status` 新增 recording-service 状态聚合。
- `cockpit-ctl health` 新增脚本化健康检查，覆盖 gateway、audio、voice、camera 和 recording。
- `运行时通信策略.md` 补充当前运行管理边界：systemd + ProcessRuntime + ModuleManager + cockpit-ctl。

### 设计决定

- 参考 `znavigator` 的运行编排思想，但当前不引入独立 manager service 或动态插件系统。
- `health` 作为部署脚本、systemd ExecStartPost/定时检查和人工排障入口；不可达或 faulted 返回非 0。

### 验证结果

- x86_64 Debug 构建通过，CTest 27/27。
- 完整 `scripts/tests/smoke.sh` 通过，现有服务间 gRPC 链路正常。

## 2026-07-07 - 录包通用事件流

### 变更内容

- `recording` 模块新增 `RecordingEvent`，支持把轻量研发事件写入 `events.jsonl`。
- `RecordingSession` 在每个会话中同时创建 `vehicle_state.jsonl` 和 `events.jsonl`。
- `RecordingService` 新增 `HandleEvent()`，为后续 camera/voice/audio 元数据进入录包预留统一入口。
- `recording_session_test` 覆盖车况数据和相机帧元数据事件共同写入。

### 设计决定

- `events.jsonl` 只保存元数据和小 payload，不保存大图像、音频或视频二进制。
- 大块数据后续通过独立文件或共享内存句柄保存，事件流只记录路径、句柄、时间戳和索引信息。

### 验证结果

- `recording_session_test` 覆盖通用事件写入和 manifest 消息计数。

## 2026-07-07 - 相机拍照、真实 UI 联调与 tidy 修复

### 变更内容

- `run_tidy.sh` 改为按当前编译数据库选择实际启用目标，排除生成代码和未启用的可选 source。
- 新增 GStreamer JPEG encoder 和 camera-service photo 子模块，从共享内存最新帧保存照片。
- camera proto、camera-ctl 和 Qt Camera 页面新增拍照接口、结果路径与错误状态。
- 新增 `run_camera_ui.sh`，要求真实设备存在且 camera-service 收到首帧后才启动 UI。
- 配置增加照片目录、JPEG 质量和最大帧龄；文件名限制防止越权路径写入。

### 设计决定

- 拍照复用 preview 共享内存，不重新打开摄像头，也不通过 gRPC 传像素。
- 编码器接口位于 camera module；GStreamer 缺失时明确报告 unavailable，保持可构建降级。
- AI 与视觉推理等待 Jetson/真实硬件阶段，不在 WSL 阶段提前接入。

### 验证结果

- x86_64 Debug 构建通过，CTest 25/25；合成 BGRx 帧已真实编码为 JPEG。
- Qt 6 cockpit-ui 编译链接及 offscreen QML 加载通过。
- 当前 WSL 会话未暴露 `/dev/video*`，真实 USB 首帧联调需重新挂载设备后执行
  `bash scripts/run-camera-ui.sh`。

## 2026-07-06 - 录包会话管理与保留策略收口

### 变更内容

- 新增 `RecordingCatalog`，启动扫描 manifest 并重建历史会话索引。
- recording gRPC 和 `recording-ctl` 新增 list、delete、prune，status 增加存储统计。
- 配置新增最大会话数和最大总字节数，启动、开始和完成录制时自动清理最旧会话。
- 损坏 manifest 作为 `corrupted` 会话暴露，不阻塞服务启动，并可通过 CLI 删除。
- 单测覆盖目录扫描、排序、统计、删除、清理和损坏隔离；smoke 覆盖真实列表与删除。

### 设计决定

- VehicleState 录包 v1 使用“原始文件权威 + 可重建目录索引”，当前不增加 SQLite 运行依赖。
- 清理只操作带 `COMPLETE` 或 `INTERRUPTED` 标记的受管目录，不触碰活动会话和外部文件。
- 相机 MP4、音频 WAV 与多源时间对齐作为独立 writer 后续实现，不宣称已随结构化录包完成。

### 验证结果

- x86_64 Debug 构建通过，CTest 24/24。
- 完整 smoke 通过，完成会话可查询、保留策略不误删、指定会话可删除。

## 2026-07-06 - VehicleState 研发录包与文件持久化

### 变更内容

- 新增平台无关 `RecordingSession`，按会话写入 manifest 和 VehicleState JSONL。
- 新增 `recording-service`，订阅 vehicle-data-service，并提供 start/stop/status gRPC 控制面。
- 新增 `recording-ctl`、recording proto、类型化配置、systemd 和安装目标。
- 完成会话使用最终目录和 `COMPLETE` 标记；异常遗留临时目录在下次启动时标记为
  `interrupted_*`。
- smoke 增加真实录制、文件非空和完成标记校验。

### 设计决定

- recording-service 属于研发诊断边界，不接收用户语音动作。
- 原始数据文件是权威数据；后续 SQLite 仅保存会话索引、查询字段和保留策略。
- 当前只接入低频 VehicleState，不提前引入通用 MessageBus 或把图像、PCM 塞进 gRPC。

### 验证结果

- x86_64 Debug 构建通过，CTest 23/23。
- 完整 smoke 通过，录制会话实际持久化 3 条 VehicleState 并生成 `COMPLETE`。

## 2026-07-06 - 产品源码迁入 cockpit 目录

### 变更内容

- 新增 `cockpit/` 产品源码根目录，将 apps、core、drivers、modules、proto 和 services 整体迁入。
- 新增 `cockpit/CMakeLists.txt` 聚合产品 target，仓库顶层继续聚合 tools、tests 和安装规则。
- 全仓 247 个 C++ include 更新为 `cockpit/...` 路径。
- clang-tidy header filter、架构文档、模块化策略和 README 同步新目录。

### 设计决定

- `cockpit/` 只放最终车机产品代码；tools、tests、docs、cmake、configs 和 scripts 保持根级。
- C++ namespace、CMake target、二进制名、配置键和部署目录均保持不变。
- 结构参考 zcarcloud/znavigator 的产品源码根，但不增加重复仓库层或历史构建兼容目录。

### 验证结果

- x86_64 Debug 全量重新配置和构建通过，CTest 22/22。
- Qt、GStreamer、whisper.cpp 完整构建 260/260，通过包含真实模型的 CTest 25/25。
- 完整 `scripts/tests/smoke.sh` 通过。
- x86_64 Release 构建、CTest 22/22 和安装包生成通过。
- 全仓 pre-commit、旧 include 路径扫描和 `git diff --check` 通过。

## 2026-07-06 - 项目文档统一中文

### 变更内容

- 根 README、架构、实现状态、配置、通信、模块化、仓库策略和语音 AI 文档改为中文。
- core、drivers、modules、proto、apps、services 和 tools 下的项目 README 全部改为中文。
- 历史变更记录移除重复的英文副标题，保留命令、路径、类名和协议名原文。
- 修正旧参考文档中已经过期的仓库路径和云端仓库描述。

### 设计决定

- 技术术语、代码标识、配置键和命令不强制翻译，避免影响检索和执行。
- `deploy/THIRD_PARTY_NOTICES.md` 属于第三方许可证说明，保持上游原文。
- 英文旧文档不逐句堆叠翻译，而是按当前代码状态收敛为中文主版本。

### 验证结果

- Markdown 英文段落和双语标题扫描完成。
- 全仓 pre-commit 和 `git diff --check` 通过。

## 2026-07-04 - 相机卡帧与断连检测

### 变更内容

- `SharedFrameReader` 新增 writer 可用性检测，camera-service 退出后 UI reader 主动断开并重连。
- Qt `CameraFrameModel` 新增 1 秒 freshness 定时器，界面区分 live、stalled 和 last frame。
- `cockpit-ctl status` 输出 camera frame health、帧龄、最后序号和源端跳帧数。
- `cockpit-ctl` 不再等待离线服务变为 ready，单机诊断在部分服务离线时立即返回。
- 新增 Qt camera model 测试，并补充共享内存 writer 退出测试。

### 设计决定

- UI freshness 属于消费者观察状态，不回写 camera-service，也不改变共享内存数据布局。
- camera-service 的 2 秒 CLI 卡帧阈值与 UI 的 1 秒显示阈值分开，诊断工具更保守。
- 保留最后一帧供用户查看，但不得把旧帧标记为实时画面。

### 验证结果

- x86_64 Debug 构建通过，CTest 22/22。
- Qt 6 完整构建通过；shared memory、vehicle model、camera model 定向测试 3/3。
- 完整 `scripts/tests/smoke.sh` 通过。
- 单独运行 camera-service 时，`cockpit-ctl status` 正确报告其他服务离线，并输出
  `frame_health: inactive`。

## 2026-07-04 - 剩余职责目录整理

### 变更内容

- `modules/audio` 拆为 `frames`、`capture`、`vad`、`playback`、`wav`，并提供独立 target。
- `modules/camera` 拆为 `frames`、`capture`、`shared_memory`，共享内存布局仍属于相机领域。
- audio-service 拆为 `capture`、`processing`、`playback`、`grpc`；camera-service 拆为
  `preview`、`control`、`grpc`，两个进程入口只负责装配。
- cockpit-ui 拆为 `vehicle` 和 `camera`，Qt model/worker 不再堆在应用根目录。
- 全仓库 include、CMake、测试和架构文档同步新路径。

### 设计决定

- 父级 `audio`、`camera`、`audio_service`、`camera_service` 只保留 INTERFACE 兼容聚合；新代码
  链接最小 target。
- `tools/topic` 已按命令文件组织，`core/runtime` 也保持单一生命周期职责，因此不继续硬拆。
- `can`、`vehicle` 和小型 service 保持扁平，只有出现多个真实职责才增加子目录。

### 验证结果

- x86_64 Debug 构建通过，CTest 22/22。
- 启用 Qt 6、GStreamer 和 whisper.cpp 的完整 `build/` 构建通过，`cockpit-ui` 成功链接。
- 完整 `scripts/tests/smoke.sh` 通过；一次 TTS 状态查询超时复跑未复现。
- `vehicle_state_model_test` 在 Qt 完整构建中通过。
- 全仓库 pre-commit 与旧路径扫描通过。

## 2026-07-04 - 相机帧健康指标

### 变更内容

- camera-service 状态新增源端跳帧数、最后帧序号、设备时间戳和服务接收时间。
- 将无效帧/发布失败与摄像头源序号跳跃分开统计，避免 `frames_dropped` 含义混杂。
- camera gRPC、`camera-ctl` 和服务单元测试同步新增字段。

### 设计决定

- 帧像素数据继续只走共享内存；gRPC 仅传输小型健康状态。
- 当前用单调递增的帧 sequence 检测源端缺口，不把消费者读取速度算作摄像头丢帧。

### 验证结果

- x86_64 Debug 构建通过，CTest 22/22。
- 完整 `scripts/tests/smoke.sh` 通过，`camera-ctl status` 已输出新增健康字段。
- 本批文件 pre-commit 与 `git diff --check` 通过。

## 2026-07-03 - Voice 职责目录与独立 Target

### 变更内容

- `modules/voice` 按 `asr`、`tts`、`assistant`、`actions`、`responses` 拆分。
- 每个 voice 子目录拥有独立 CMake target，父级 `voice` 改为兼容聚合 target。
- `voice-interaction-service` 按 `interaction`、`audio`、`vehicle`、`hmi`、`grpc` 拆分，
  `main.cc` 只负责进程装配。
- 全仓库 include、测试和文档同步到新路径。

### 设计决定

- 参考 zelos 的职责目录和独立 target，但不照搬大型项目层级。
- 目录名必须直接说明用途，不使用 `base`、`common`、`misc` 等模糊收纳目录。
- `can`、`vehicle` 和小型 service 继续保持扁平，出现多个真实职责后再拆。

### 验证结果

- 新分层完成 x86_64 Debug 构建，CTest 22/22，完整 WSL smoke 通过。
- 启用 whisper.cpp 的旧构建目录重新配置并完成 217 个构建步骤，真实模型测试通过，
  耗时约 24.9 秒。
- 旧 include 路径扫描清零，pre-commit 检查通过。

## 2026-07-03 - 语音输出健康指标与退避

### 变更内容

- 语音输出状态新增 consecutive failures 和 last success timestamp。
- AudioSpeechClient 区分传输失败与远端队列满，避免把 RESOURCE_EXHAUSTED 误判为掉线。
- AudioTranscriptClient 未连接重试采用指数退避，从配置基准开始，最高 5 秒。
- voice-ctl 展示输出健康指标，protobuf 使用追加字段保持兼容。

### 设计决定

- 最近成功时间表示响应已被 audio-service 接收，不表示扬声器已经播放完成。
- 正常建立连接后立即重置退避，重试等待保持可被 SIGTERM 快速打断。

### 验证结果

- x86_64 Debug 构建通过，CTest 22/22。
- audio-service 缺失 2.2 秒内 transcript 探测 4 次；输出显示 unavailable、连续失败 1、
  最近成功 0，进程退出耗时 13 ms。
- 完整 WSL smoke 通过；正常投递后 available=yes、连续失败归零、最近成功时间非零。
- protobuf 生成和 pre-commit 检查通过。

## 2026-07-03 - 可取消语音输出与可用状态

### 变更内容

- VoiceResponseSink 增加默认 Stop 生命周期钩子，异步包装器停止时先取消下游调用。
- AudioSpeechClient 跟踪活动 gRPC context，停止时通过 TryCancel 中断等待。
- AudioTranscriptClient 在建立长期流前执行 250 ms channel 连接预检，避免无服务时卡满 10 秒。
- 语音状态新增 speech output available 和 reconnects，并由 voice-ctl 展示。
- 异步输出测试新增阻塞下游取消和退出耗时验证。

### 设计决定

- gRPC channel 自身负责连接恢复，业务层只记录从不可用恢复为可用的次数。
- protobuf 只追加字段，保持已有客户端兼容。

### 验证结果

- x86_64 Debug 构建通过，CTest 22/22。
- audio-service 缺失时状态显示 `speech output available: no`，voice service 退出从约 9.9 秒
  缩短到 17 ms。
- 完整 WSL smoke 通过；audio-service 正常时显示 `speech output available: yes`，三次异步
  播报均接收成功。
- protobuf 生成、异步阻塞取消测试和服务 worker 回收均通过。

## 2026-07-03 - 异步语音响应输出

### 变更内容

- 新增通用 `AsyncVoiceResponseSink`，通过有界队列和独立 worker 调用实际输出端。
- voice-interaction-service 使用异步包装后的 AudioSpeechClient，命令响应不再等待 TTS RPC。
- 新增阻塞下游测试，覆盖非阻塞提交、容量上限、丢弃指标和队列消费。

### 设计决定

- 异步能力属于 voice module，不绑定 gRPC，后续 Android bridge 或本地 TTS 可以复用。
- 停止时丢弃尚未发送的播报并计数，等待正在执行的单次下游调用结束。

### 验证结果

- x86_64 Debug 构建通过，CTest 22/22。
- 阻塞下游测试确认提交不等待下游，容量为 1 时第三条请求被拒绝并计入 dropped。
- 完整 WSL smoke 通过，三条语音动作均立即返回，异步 TTS 接收 3/3、无失败和丢弃。
- voice-interaction-service 停止后 worker 正常退出，无悬挂进程。

## 2026-07-03 - WSL Null Audio Capture Pacing

### 变更内容

- ALSA capture adapter 对 `null` 输入设备按采样率模拟实时时钟。
- 防止 WSL smoke 中 null plugin 无阻塞读取占满 CPU、压垮 VAD consumer 和 gRPC 控制面。
- voice-ctl 控制 RPC deadline 调整为覆盖 gateway 重试和 TTS 下游调用的完整超时预算。

### 设计决定

- 节流只作用于明确的 `null` 测试设备；真实麦克风继续由 ALSA/硬件时钟驱动。
- 该行为属于设备 adapter，不进入平台无关的 `AudioCaptureStream`。

### 验证结果

- x86_64 Debug 增量构建通过，CTest 21/21。
- 完整 `scripts/tests/smoke.sh` 通过；null capture 稳定在约 16,000 PCM frames/s、零丢帧。
- 车辆状态查询、HMI 动作、TTS、camera 控制和 topic 链路均通过。

## 2026-07-03 - 多架构构建目录与 Jetson 工具链

### 变更内容

- 构建目录统一为 `build/<目标架构>-<debug|release>`。
- `scripts/build.sh` 新增 `--arch`、`--type`、`--no-test` 和 CMake 参数透传。
- 新增 Jetson AArch64 CMake toolchain 和 sysroot 同步脚本。
- smoke、vcan、Qt UI 和 clang-tidy 脚本同步使用新的默认 Debug 目录。
- 打包脚本改为读取 CMake 目标架构，支持在 x86_64 上生成正确命名的 ARM64 包。

### 设计决定

- 目录名只描述目标架构和构建类型，不混入构建机器或设备型号。
- Jetson 原生构建仍是当前默认发布方式；交叉编译要求匹配 JetPack/L4T 的 sysroot。
- x86_64 到 ARM64 的交叉构建不运行目标程序测试。

### 验证结果

- x86_64 Debug 和 Release 均完成 178 个构建步骤，CTest 均为 21/21 通过。
- Release 包成功生成为 `cockpit-system-0.1.0-linux-x86_64.tar.gz`，目标架构和构建机架构
  均正确写入 `BUILD_INFO.json`。
- 发布包 SHA256 清单全部通过，可执行文件为 x86-64 ELF，RUNPATH 为 `$ORIGIN/../lib`。
- ARM64 工具链完成参数与失败路径检查；真实交叉链接等待 Jetson sysroot。
- 完整 smoke 初次暴露 null capture 高吞吐和控制端超时预算问题，已在同日后续批次修复。

## 2026-07-03 - Release Packaging and Jetson Layout

### 变更内容

- 参考 znavigator 的 build/install/package 分层，新增 CMake Runtime install 规则。
- 新增 `scripts/package.sh`，生成 `stage/` 安装树和 `dist/*.tar.gz` 发布包。
- 发布包包含正式二进制、可选 whisper/GGML 动态库、配置模板、systemd、构建信息和校验清单。
- 新增 install、rollback 和 healthcheck 脚本，默认安装根目录为 `/cockpit-system`。
- systemd 路径统一为 `/cockpit-system/current`，新增核心服务 target 和缺失单元。
- 新增 Jetson 部署文档和第三方依赖声明。

### 设计决定

- 源码、构建目录和车端运行目录分离，不复制整个 `build/`。
- 版本化程序位于 `releases`；配置、模型、数据和日志跨版本保留。
- Whisper 模型不进入默认程序包，避免每次升级重复传输大模型。
- 第一阶段不加入配置加密、Breakpad 或 debug symbol 分包。

### 验证结果

- Release 构建通过，包含真实 Whisper 推理的 CTest 23/23。
- `scripts/package.sh` 成功生成 x86_64 staging tree 和 tar.gz。
- 可执行文件 RUNPATH 为 `$ORIGIN/../lib`，whisper/GGML RUNPATH 为 `$ORIGIN`；动态库全部
  从发布包解析，不引用 build 目录。
- `SHA256SUMS` 全部通过。
- 临时根目录完成两次安装，验证 `current` 切换、配置保留、`.new` 模板和 healthcheck。

## 2026-07-01 - Optional whisper.cpp ASR Provider

### 变更内容

- 新增可选 `WhisperSpeechRecognizer`，把完成的 16 kHz mono speech segment 转成 float PCM
  并调用 whisper.cpp 本地推理。
- CMake 新增 `BUILD_WHISPER_CPP_ASR` 和 `WHISPER_CPP_DIR`，默认关闭且不增加默认依赖。
- voice 配置新增 ASR 模型路径、语言和线程数。
- audio-service 根据 `asr_provider` 选择 mock 或 whisper.cpp；未编译 provider 时明确拒绝启动。
- 补充配置、构建和模型资产说明。

### 设计决定

- whisper.cpp 作为可选 adapter，不复制第三方源码、不提交模型文件。
- 默认构建继续使用 mock，保证 WSL、CI 和无模型环境可重复构建。
- ASR 只消费 VAD 完成后的 speech segment，不直接访问 ALSA 或采集线程。

### 验证结果

- 默认 mock 构建通过，CTest 22/22。
- `pre-commit run` 针对本批文件通过。
- 配置选择 `whisper_cpp` 但构建未启用 provider 时，audio-service 明确报错并返回 1。
- 下载并校验官方多语言 `ggml-small.bin`，SHA-1 为
  `55356645c2b361a969dfd0ef2c5a50d530afd8d5`。
- whisper.cpp Release CLI 在 WSL CPU 上成功识别 11 秒官方样例，总耗时约 3.8 秒。
- cockpit-system 启用 `BUILD_WHISPER_CPP_ASR` 后成功编译 adapter；真实模型集成测试通过，
  CTest 23/23。
- 中文录音、真实麦克风链路和 Jetson CUDA 性能仍待验证。

## 2026-07-01 - Runtime Module Status Surface

### 变更内容

- `common.proto` 新增通用 runtime module 状态枚举和消息。
- audio/camera status 增加 module 列表，暴露 `audio-capture` 和 `camera-preview` 生命周期。
- `cockpit-ctl status` 展示各服务内部 module 的 created/running/stopped/failed 状态。
- 修正新建 gRPC channel 快速失败的问题，在 deadline 内等待服务连接就绪。
- audio/camera 单元测试增加 module 状态断言。

### 设计决定

- module 状态复用现有 service status RPC，不新增独立 runtime service。
- 当前只暴露只读状态，不开放远程 module restart。
- service 仍是进程与资源边界，module 状态只是进程内部诊断信息。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 22/22。
- 临时启动 audio-service 和 camera-service 后，`cockpit-ctl status` 成功显示
  `audio-capture=created` 和 `camera-preview=created`。

## 2026-07-01 - Cockpit Diagnostics CLI

### 变更内容

- 新增 `cockpit-ctl status` 顶层诊断工具。
- 通过现有 gRPC 控制面汇总 gateway、audio、voice 和 camera 状态。
- 服务未启动时显示 `available: no` 和 RPC 错误，不让整条诊断命令失败。
- 新增 `cockpit-ctl status --watch` 持续刷新模式。
- 支持 `--interval SEC` 自定义刷新间隔，默认 2 秒。
- SIGINT/SIGTERM 可干净退出 watch 模式。
- README 增加 `cockpit-ctl status` 常用命令。

### 设计决定

- 当前保持只读诊断，不直接暴露 restart 或控制命令。
- 不新增 proto，复用各 service 已有 `GetStatus` / `GetLatestVehicleState`。
- 先把分散的 probe/ctl 能力收成一个入口，后续再接 runtime module status。
- watch 模式用 ANSI 清屏 + 每秒检查停止信号，不做复杂的 curses 界面。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 22/22。
- `build/bin/cockpit-ctl status --config configs/config.yaml` 在服务未启动时正常输出
  unavailable 状态。
- `build/bin/cockpit-ctl status --watch --interval 1` 每秒刷新正常，Ctrl+C 退出打印 stopped。

## 2026-07-01 - Audio Capture Runtime Module

### 变更内容

- audio-service 新增 `AudioCaptureModule`，把 `AudioCaptureStream` 包装成 runtime module。
- `AudioService` 内部使用 `ModuleManager` 管理采集流启动和停止。
- VAD、speech segment、ASR 处理逻辑保持在 audio-service 内部，不提前拆成独立 module。

### 设计决定

- 只模块化真正占用麦克风资源且有独立生命周期的 capture stream。
- 音频 PCM 数据仍走进程内 ring buffer，不引入 gRPC 或跨进程传输。
- 当前目标是统一资源生命周期，不扩大 service 数量。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 22/22。

## 2026-07-01 - Camera Service Runtime Module

### 变更内容

- camera-service 新增 `CameraPreviewModule`，把 GStreamer preview source 包装成 runtime module。
- `CameraService` 内部使用 `ModuleManager` 管理预览启动、停止和失败回滚。
- 对外 gRPC/CLI 行为保持不变，仍然由 camera-service 独占摄像头设备。

### 设计决定

- 只把 preview pipeline 收进进程内 lifecycle，不新增 camera 子服务。
- 共享内存帧数据面保持不变，runtime module 只负责生命周期。
- 这一步用于验证 runtime 层在真实服务中可用，避免停留在空框架。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 22/22。

## 2026-07-01 - Runtime Module Lifecycle

### 变更内容

- 新增 `runtime::Module`、`runtime::BasicModule` 和 `runtime::ModuleManager`。
- 支持模块按注册顺序启动、按反向顺序停止。
- 启动失败时自动回滚已启动模块。
- 新增 `module_manager_test` 覆盖启动、停止、状态查询和失败回滚。

### 设计决定

- 先提供轻量进程内生命周期，不引入插件加载、跨进程调度或复杂 scheduler。
- runtime 只描述模块生命周期，不持有 audio/camera/voice 等领域逻辑。
- 该层用于后续逐步收拢服务内部模块，而不是新增更多 service。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 22/22。

## 2026-07-01 - Architecture Blueprint v0.4

### 变更内容

- 将总体架构蓝图从 `architecture_refined_v0.3.md` 更新为 `architecture_refined_v0.4.md`。
- 同步当前工程状态：`core/ipc` 通用共享内存、camera-service 共享内存数据面、Qt 相机控制面、
  audio/voice/camera/vehicle 当前模块边界。
- 更新 README 和 docs 索引，统一指向 v0.4 架构蓝图。

### 设计决定

- 继续保持单仓库、内部模块化，避免过早拆成多个代码库。
- 明确少量 device-owner process + 本地 runtime/event/ipc 的路线，避免把内部高频链路做成普通微服务。
- 明确 gRPC 主要用于低频控制面，音频帧和相机帧等高频数据优先走本地队列或共享内存。

### 验证结果

- 文档入口已从 v0.3 同步到 v0.4。
- 本批为文档更新，无需重新编译代码。

## 2026-07-01 - Qt Camera Lifecycle Controls

### 变更内容

- 新增 `CameraControlModel`，在独立线程调用 camera-service gRPC 控制面。
- Camera tab 增加设备刷新与选择、分辨率、FPS、START/STOP 控件。
- UI 启动时同时拉取设备列表和当前 preview 状态，可接管脚本已启动的相机。
- 控制失败信息直接反馈到 Camera tab，命令执行期间禁用冲突操作。

### 设计决定

- 高频图像继续走共享内存，低频设备与生命周期命令继续走 gRPC。
- QML 只绑定 `CameraControlModel`，不直接持有 gRPC stub 或执行阻塞调用。
- 控制模型与帧 reader 分离，避免 RPC 延迟阻塞相机数据面。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 21/21。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `run_cockpit_ui.sh --offscreen` 成功加载设备、分辨率、FPS 和启停控件，限时退出无 QML
  或工作线程清理错误。

## 2026-07-01 - Qt Shared-Memory Camera View

### 变更内容

- cockpit-ui 新增后台 `CameraFrameClient`，从 camera-service 共享内存读取最新帧。
- 新增 `CameraImageProvider` 和 `CameraFrameModel`，通过 queued invocation 安全更新 UI。
- QML 增加 Dashboard/Camera tabs，显示相机连接、分辨率、帧序号和实时画面。
- `run_cockpit_ui.sh` 管理 camera-service，等待控制面就绪，并在设备存在时自动启动预览。

### 设计决定

- QML 不访问共享内存、V4L2 或 GStreamer；硬件和 IPC 仍由 C++ 层负责。
- reader 在工作线程按 generation 读取，只有新帧才转换和刷新 `image://camera`。
- 当前 UI 支持 GStreamer 默认输出的 BGRx/RGB；压缩格式和 YUV 转换留在采集 pipeline。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 21/21。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `run_cockpit_ui.sh --offscreen` 完整启动 vehicle、gateway、camera-service 和 cockpit-ui；
  QML Camera tab 加载成功，限时退出后无残留进程或 QML context 错误。

## 2026-07-01 - Camera Shared-Memory Double Buffer

### 变更内容

- 新增 `camera_shm` 模块，提供 `SharedFrameWriter` 和 `SharedFrameReader`。
- 新增通用 `core/ipc/SharedMemoryRegion`，统一 POSIX shared memory 创建、打开、映射和清理。
- 使用 POSIX shared memory、双帧槽和 `PTHREAD_PROCESS_SHARED` 读写锁传输相机帧。
- camera-service 启动时创建共享内存 sink，帧像素不经过 gRPC。
- 配置增加 `services.camera.frame_transport/shared_memory_name/max_frame_bytes`。
- 新增跨映射测试，覆盖双槽发布、读取、generation、payload 和超限拒绝。

### 设计决定

- 通用映射生命周期位于 `core/ipc`；相机双槽布局仍保留在 `modules/camera`。
- writer 写入非活动槽后切换 active slot；reader 只在复制当前帧时持有进程共享读锁。
- camera-service 仍依赖 `CameraFrameSink` 抽象，GStreamer 继续保持可选依赖。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 20/20。
- 分层调整后 `bash scripts/build.sh` 通过，CTest 21/21。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `bash scripts/tests/smoke.sh` 单独运行通过；camera-service 正常创建并清理共享内存区域。

## 2026-07-01 - Camera Frame Sink and Latest Buffer

### 变更内容

- 新增 `CameraFrameSink`，把摄像头采集源与帧消费者解耦。
- 新增线程安全的 `LatestFrameBuffer`，固定只保存最新一帧，并记录发布、替换和 generation。
- 相机回调改为按值传递，GStreamer 生成的 `CameraFrame` 通过 move 交给 service 和 sink。
- camera-service 默认使用 latest-frame sink，采集到的帧不再只做计数后丢弃。
- 新增 `latest_frame_buffer_test`，覆盖无效帧、覆盖语义、读取、状态指标和清理。

### 设计决定

- 当前数据面先保持进程内，不提前引入复杂共享内存协议。
- latest-frame 模型适合预览：消费者变慢时覆盖旧帧，不反压采集线程，也不形成无界视频队列。
- 生产路径移动帧所有权；只有消费者调用 `ReadLatest()` 时才复制图像数据。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 19/19。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `bash scripts/tests/smoke.sh` 通过；当前会话无 `/dev/video*` 时，camera 控制面正常降级运行。

## 2026-06-25 - Camera Service Capture Pipeline

### 变更内容

- camera-service 通过 `CameraPreviewSource` 持有本地采集 pipeline，默认使用可选的
  `GstreamerPreviewPipeline`。
- `StartPreview` 现在真正启动 `v4l2src -> appsink`，`StopPreview` 释放 pipeline 和摄像头设备。
- 有效帧和无效帧分别累计到 `frames_received`、`frames_dropped`，通过原有 gRPC status 查询。
- camera service 单元测试注入 fake preview source，覆盖真实启停调用、配置传递和帧计数，且不依赖硬件。

### 设计决定

- gRPC 继续只作为控制面；视频帧停留在进程内 callback，后续再接 HMI 或共享内存数据面。
- camera-service 依赖抽象采集接口，不把 GStreamer 类型暴露给服务 API，方便 Jetson 后续替换 pipeline。
- 新 start 请求先停止旧 pipeline，确保服务状态和实际设备生命周期一致。

### 验证结果

- `pre-commit run` 针对本批文件通过。
- `bash scripts/build.sh` 通过，CTest 18/18。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `bash scripts/tests/smoke.sh` 通过；当前会话无 `/dev/video*` 时，camera 控制面仍正常运行。

## 2026-06-24 - Camera Control CLI

### 变更内容

- 新增 `tools/camera-ctl`，支持通过 gRPC 调用 camera-service 的 `list/status/start/stop`。
- `scripts/tests/smoke.sh` 启动 camera-service，并用 camera-ctl 验证 status/list 控制面。
- camera-service 进程加入 smoke cleanup，避免失败时残留后台进程。

### 设计决定

- camera-ctl 只验证控制面，不传输视频帧；帧数据仍保留给后续本地 pipeline/shared memory。
- 默认 smoke 不要求真实摄像头存在；无 `/dev/video*` 时 list 输出为空仍视为软件链路可用。

### 验证结果

- `pre-commit run` 通过。
- `bash scripts/build.sh` 通过，CTest 18/18。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `bash scripts/tests/smoke.sh` 通过，camera-service status/list 控制面已进入默认 smoke。

## 2026-06-24 - Camera Service Skeleton

### 变更内容

- 新增 `proto/camera.proto`，定义 camera control gRPC 接口：list/start/stop/status。
- 新增 `processes/camera`，包含可测试的 `CameraService` 核心、gRPC 控制面和进程入口。
- 配置增加 `services.camera.grpc.listen_address`，默认 `127.0.0.1:50054`。
- 新增 `camera_service_test` 覆盖设备枚举、preview start/stop/status 和不可用设备拒绝。

### 设计决定

- 第一版 camera-service 只建立控制面和生命周期，不通过 gRPC 传视频帧。
- 真实帧流后续接 GStreamer pipeline 或共享内存；当前先保持和自动驾驶/车载系统的控制面/数据面分离。

### 验证结果

- `pre-commit run` 通过。
- `bash scripts/build.sh` 通过，CTest 18/18。
- `pre-commit run clang-tidy --hook-stage manual -a` 通过。
- `bash scripts/tests/smoke.sh` 通过；当前环境未暴露 `/dev/video*` 时，camera-probe 正常报告无 V4L2 设备。

## 2026-06-24 - Clang-Tidy Commit Gate

### 变更内容

- 收敛 `.clang-tidy` 规则，关闭不适合作为普通 commit 门禁的风格噪声检查。
- 修复剩余 clang-tidy warning：signal handler 返回值检查、logger 静态初始化、topic 工具命名空间使用。
- `.pre-commit-config.yaml` 中的 `clang-tidy` 从 manual hook 提升为普通 commit hook。
- 根据实际开发体验调整：`clang-tidy` 保持 manual hook，普通 commit 不强制执行；全仓库静态检查手动运行。

### 设计决定

- commit gate 保留能发现实际问题的检查，DTO public 字段、日志宏、Linux/Qt/第三方库风格冲突类检查不作为阻塞项。
- `clang-tidy` 仍通过 `scripts/lint.sh` 全仓库运行，确保普通提交前能发现跨文件问题。

### 验证结果

- `bash scripts/lint.sh` 通过，exit status 0，项目 warning/error 清零。
- `pre-commit run` 通过，普通 commit 阶段不执行 `clang-tidy`。
- `pre-commit run clang-tidy --hook-stage manual -a` 可手动执行完整 clang-tidy。
- `bash scripts/build.sh` 通过，CTest 17/17。

## 2026-06-23 - Clang-Tidy Gate Cleanup

### 变更内容

- 清理剩余 clang-tidy error：camera GStreamer pipeline、audio-service、audio-probe 和 voice-ctl。
- GStreamer preview pipeline 合并重复分支，析构路径改为调用非虚拟资源释放函数。
- audio-service 去除 trivially-copyable 配置上的无效 `std::move`，并对非性能关键服务类的 padding analyzer 提示做局部抑制。
- audio/voice 工具对 protobuf enum sentinel 使用 `default` 兜底为 `unknown`。
- `.clang-tidy` 禁用 `clang-analyzer-cplusplus.NewDeleteLeaks`，避免 Qt 系统头 `qobjectdefs.h` 的第三方误报卡住项目 tidy。

### 设计决定

- 当前目标是让 clang-tidy 手动门禁稳定通过；大量 warning 后续按模块逐步收敛。
- 第三方库/系统头误报不作为项目修复范围。

### 验证结果

- `pre-commit run` 针对本批修改文件通过。
- `bash scripts/build.sh` 通过，CTest 17/17。
- `bash scripts/lint.sh` 通过，exit status 0；当前项目 warnings 约 1206 个，后续分批治理。

## 2026-06-23 - First Clang-Tidy Cleanup

### 变更内容

- `scripts/lint.sh` 的 header filter 收窄到项目源码目录，避免 clang-tidy 扫描 `build/proto/generated` 生成代码。
- 修复 `core/logging` 的默认日志大小参数，避免 int 乘法后再隐式扩宽。
- 修复 `modules/audio` 中对 trivially-copyable `std::array` 使用无效 `std::move` 的问题。
- 修复 WAV chunk padding 计算中先窄类型相加再转宽类型的潜在问题。

### 设计决定

- 生成代码不纳入 tidy 统计和修复范围；proto 生成物的问题由生成器负责。
- 第一批只清 `core/` 和 `modules/audio/` 的 tidy error，不处理大范围 warning。

### 验证结果

- `pre-commit run --files cockpit/core/logging/logger.h scripts/lint.sh` 通过。
- `bash scripts/build.sh` 通过，CTest 17/17。
- `bash scripts/lint.sh` 排除生成代码后，error 从 40 降到 20；`core/` 和 `modules/audio/` error 已清零。

## 2026-06-23 - Voice Vehicle Status Retry

### 变更内容

- `GatewayVehicleStatusClient` 查询车辆状态时增加短时间重试，避免 voice smoke 在 gateway 刚启动或车辆状态刷新边界上偶发失败。

### 设计决定

- 保留 gateway 对 stale vehicle state 的拒绝逻辑，voice 查询端只负责等待短暂的新鲜数据窗口。
- 重试逻辑只用于用户语音查询车辆状态，不改变车辆数据发布频率和 topic 订阅行为。

### 验证结果

- `pre-commit run --files processes/interaction/gateway_vehicle_status_client.cc docs/变更记录.md` 通过。
- `bash scripts/build.sh` 通过，CTest 17/17。
- `bash scripts/tests/smoke.sh` 通过，voice vehicle status 查询返回 live response。

## 2026-06-23 - Clang-Tidy Manual Hook

### 变更内容

- 新增 `scripts/lint.sh`，统一通过 `build/compile_commands.json` 执行 clang-tidy。
- `.pre-commit-config.yaml` 增加 `clang-tidy` manual hook，手动运行时执行静态检查，普通 commit 暂不强制。
- `install_ubuntu_deps.sh` 增加 `clang-tidy` 依赖。

### 设计决定

- 当前 tidy 先作为手动检查入口，避免一次性把历史代码全部卡在静态检查问题上。
- 普通提交继续默认执行基础文件检查和 `clang-format --dry-run -Werror`。

### 验证结果

- `pre-commit run` 按正常提交模式通过，manual hook 不影响普通提交。

## 2026-06-23 - Pre-commit and Camera Permission Cleanup

### 变更内容

- 确认 `ffz` 已属于 `video` 组；若已有 `/dev/video*` 但仍权限不足，需要刷新当前 shell 组权限。
- 整理 `.pre-commit-config.yaml`，参考 znavigator 的检查思路，加入基础文件检查和本地
  `clang-format --dry-run -Werror`。
- `install_ubuntu_deps.sh` 增加 `pre-commit` 和 `clang-format`。
- README 和 docs README 增加 pre-commit 启用说明。

### 设计决定

- 当前先不上 cpplint/clang-tidy pre-commit gate，避免把已有工程一次性卡在大范围风格问题上。
- clang-format 使用本地系统命令，减少 pre-commit 首次运行对 clang-format mirror 下载的依赖。

### 验证结果

- `pre-commit validate-config` 通过。
- `pre-commit run` 按正常提交模式通过；`pre-commit run --all-files` 会暴露历史 C++ 文件格式问题，
  后续如需全仓库格式化应单独开一批处理。
- `git diff --check` 通过。
- `bash scripts/build.sh` 通过，CTest 17/17。
- `bash scripts/tests/smoke.sh` 通过。

## 2026-06-23 - 摄像头预览探针

### 变更内容

- 新增可选 `tools/camera-preview-probe`，在 `gstreamer_camera` target 可用时编译。
- 工具通过 `GstreamerPreviewPipeline` 从 `/dev/video*` 抓取指定帧数，输出尺寸、stride、帧大小和 FPS。
- 修正 GStreamer preview pipeline 的时间工具 include，使其在安装 GStreamer dev 包后能进入真实编译路径。
- `camera-probe --list` 现在会列出已发现但无权限查询的 `/dev/video*`，不再误报为无设备。
- preview pipeline 启动失败时会读取 GStreamer bus error，便于定位 permission/caps/plugin 问题。

### 设计决定

- `camera-probe` 负责设备/格式探测；`camera-preview-probe` 负责真实 preview pipeline 抓帧验证。
- 预览探针不写文件、不录像，只验证 USB 摄像头到 `appsink` 的用户可见预览链路。
- 当前 WSL 未出现 `/dev/video*` 时不把 preview probe 放进默认 smoke，避免依赖物理硬件。

### 验证结果

- `git diff --check` 通过。
- `bash scripts/build.sh` 通过，GStreamer dev 包安装后 `gstreamer_camera` 和 `camera-preview-probe` 已进入真实编译路径。
- `bash scripts/tests/smoke.sh` 通过，`camera-probe` 在无权限时能输出 `/dev/video* Permission denied`，默认链路不要求真实抓帧。
- 使用 `newgrp video` 刷新组权限后，USB UVC 摄像头验证通过：`/dev/video0` 为 capture 节点，支持
  YUYV、MJPG、NV12，分辨率包含 640x480、1280x720、1920x1080；`camera-preview-probe` 成功抓取
  30 帧 640x480 BGRx 预览帧。

## 2026-06-23 - 摄像头媒体模块边界

### 变更内容

- 新增 `modules/camera`，提供 `CameraFrame`、`CameraPreviewConfig` 和 `CameraPreviewSource`。
- 新增可选 `GstreamerPreviewPipeline`，参考旧项目 `CameraDevice` 的 `v4l2src + appsink` 思路，
  先实现 preview frame callback，不包含拍照/录像业务。
- `gstreamer_camera` target 仅在检测到 `gstreamer-1.0`、`gstreamer-app-1.0`、`gstreamer-video-1.0`
  时启用，避免当前 WSL 缺依赖时影响主构建。
- `install_ubuntu_deps.sh` 增加 GStreamer 开发包和常用插件。

### 设计决定

- `drivers/v4l2` 负责 Linux 设备探测，`modules/camera` 负责产品侧帧模型和预览 pipeline 边界。
- 预览是用户可见媒体能力；录包、长期录像、研发数据采集后续仍属于 recording/diagnostics。
- 先用 copied `CameraFrame` 保持简单；后续大帧/高频视频流再升级 shared memory 或 zero-copy。

### 验证结果

- `bash scripts/build.sh` 通过，新增 `camera_frame_test` 后 CTest 为 17/17。
- `bash scripts/tests/smoke.sh` 通过，camera-probe、音频、语音、车身和 topic 链路正常。

## 2026-06-23 - V4L2 摄像头探测基础

### 变更内容

- 参考旧项目中的裸 V4L2 采集、ROS 摄像头采集线程和 `VechicleSystem-main` 的 GStreamer
  `CameraDevice`，先落地更底层的 Linux V4L2 adapter。
- 新增 `drivers/v4l2`，支持 `/dev/video*` 发现、`VIDIOC_QUERYCAP` 能力查询、像素格式枚举和
  离散分辨率列表。
- 新增 `tools/camera-probe`，支持 `--list` 和 `--device /dev/video0 --formats`。
- smoke 增加 `camera-probe --list`，在 WSL 无摄像头时也应正常返回。

### 设计决定

- V4L2 设备访问属于 `drivers/`，不放进 voice 或 UI。
- 当前只做设备探测和能力查询，不在第一步启动视频流。
- 后续 GStreamer preview/photo/recording 应作为 media/camera 模块继续设计，避免把研发录包和用户语音动作混在一起。

### 验证结果

- `bash scripts/build.sh` 通过，包含 `camera-probe` 构建和 16/16 CTest。
- `build/bin/camera-probe --list --config configs/config.yaml` 在当前 WSL 无摄像头环境正常输出
  `no V4L2 video devices found`。
- `bash scripts/tests/smoke.sh` 通过，camera-probe 已纳入 smoke 链路。

## 2026-06-23 - HMI 命令交接边界

### 变更内容

- 新增 `HmiCommandProvider`，用于 open camera preview 和 play music 等用户可感知 HMI 命令。
- CockpitActionDispatcher 可选接入 HMI provider；未配置时返回 not_implemented。
- voice-interaction-service 接入 `LocalHmiCommandProvider`，当前只记录 handoff，不执行 Android/UI 动作。
- smoke 增加 play music 和 open camera handoff 校验。
- smoke 在语音车辆状态前等待 gateway 新鲜车辆状态，并断言 query_vehicle_status succeeded。
- 测试覆盖 HMI 命令成功、未配置和失败路径。

### 设计决定

- C++ voice 模块不实现 Android 音乐播放器，也不直接操作 UI App。
- 播放音乐、打开摄像头预览等动作只作为 typed command 交给未来 Qt/Android/HMI bridge。
- 本地 provider 是调试占位实现，返回“已记录命令”，不声称真实 App 已打开。

### 验证结果

- cockpit_action_dispatcher_test 和完整 smoke 覆盖 HMI handoff 行为。

## 2026-06-23 - 语音动作与录包边界

### 变更内容

- 从 voice intent/action 白名单移除 start/stop recording。
- mock assistant 不再把录包命令识别成用户语音动作。
- 文档明确录包、数据采集和研发记录属于 diagnostics/recording 边界，不属于 voice 主线。

### 设计决定

- voice-interaction-service 只处理用户语音交互。
- 录包、雷达/音视频研发采集、数据记录后续如果需要，应走独立 recording/diagnostics 边界和研发工具。

### 验证结果

- 更新 mock voice 和 voice interaction 测试，确保录包短语不再触发 voice action。

## 2026-06-23 - 同进程事件队列

### 变更内容

- 新增 `core/event/EventQueue<T>`，用于低频 typed event 的同进程投递。
- 支持 bounded capacity、TryPop、WaitPop、WaitPopFor、Close、Reset 和 drop_count。
- voice-interaction-service 使用 EventQueue 将 transcript 接收和 intent/action 处理解耦。
- voice gRPC status 和 voice-ctl 暴露 transcript event drop 指标。
- 新增 event CMake target 和 `event_queue_test`。

### 设计决定

- 该队列不是高频数据面，不替代 audio SPSC ring，也不承载 PCM/图像大 buffer。
- 当前只做同进程事件队列，不做跨进程 IPC、shared memory 或网络 message bus。
- gRPC transcript stream 线程只投递事件，voice worker 线程串行执行 assistant、action 和 TTS。

### 验证结果

- `event_queue_test` 覆盖顺序、溢出、move-only 事件、等待、关闭和 reset。
- `voice_interaction_service_test` 覆盖异步 transcript 入队、响应发布和停止后拒绝入队。

## 2026-06-23 - znavigator 模块化参考固化

### 变更内容

- 补充 znavigator 目录结构到 cockpit-system 当前目录的映射关系。
- 明确 znavigator 更像运行时/模块编排器，不是自动驾驶算法栈模板。
- 记录 `library/*_entry`、`dl_api`、`zoe_*`、`transfer/restful` 对当前项目的参考价值。
- 补充 `ipc_connector`、`socketpair`、`ProtocolUnit` 和 `application.yaml` channel wiring 的实际阅读结论。
- 明确 core、modules、drivers、services、tools、proto、configs 的放置规则。
- 增加 cockpit-system 通信规则：gRPC 做控制/调试，高频数据使用本地队列、ring 或后续共享内存。
- 新增 runtime 通信策略文档，明确同线程、同进程、跨进程、跨机器的传输选择。
- 修正 modules/drivers README 中 audio、voice、alsa 已落地后的过期描述。

### 设计决定

- 继续参考 znavigator 的薄入口、小内部库、显式模块边界思路。
- 不切换到 xmake，也不照搬 `library/*_entry` 目录命名；当前服务边界用 `processes/*` 表达。
- 当前阶段不做 `dlopen` 插件系统和通用 REST 控制面；先用静态服务、systemd、smoke 脚本和 typed gRPC。

### 验证结果

- 本批为文档和结构规则更新，无需重新编译。

## 2026-06-23 - 真实车辆状态语音动作

### 变更内容

- gateway 新增 GetLatestVehicleState unary RPC，并拒绝缺失或超过 2 秒的旧快照。
- 新增平台无关 VehicleStatusProvider 和 CockpitActionDispatcher。
- voice service 新增 GatewayVehicleStatusClient，query_vehicle_status 不再使用 mock action。
- 动作结果成为最终 response text，可直接播报实时车速、电量和 gear code。
- voice interaction 配置新增 gateway_address；其他动作继续返回 not_implemented。
- smoke 改为 audio、vehicle、gateway、voice 四服务并行联调。

### 设计决定

- voice 只访问 gateway 聚合接口，不直接依赖 vehicle-data-service 的底层 streaming API。
- 每种真实动作使用独立类型化 provider，不引入通用 shell 或任意 RPC 调用能力。
- 车辆 gear 尚无正式信号规范，因此播报 gear code，不猜测 P/R/N/D 语义。

### 验证结果

- dispatcher 测试覆盖真实快照格式化、provider 失败、缺失和未实现动作。
- CTest 15/15 通过。
- 完整 smoke 返回实时车辆状态，action succeeded、speech accepted，TTS played=2/failed=0。

## 2026-06-23 - TTS 与扬声器输出

### 变更内容

- 新增平台无关 SpeechSynthesizer、AudioPlayer 和 VoiceResponseSink 接口。
- 新增确定性 mock TTS，生成 16 kHz、mono、PCM16 提示音。
- audio-service 新增容量为 8 的异步 SpeechOutput 队列和 Speak(text) gRPC。
- 新增 ALSA AudioPlayer；audio-probe 支持 speak 并展示 TTS 指标。
- voice-interaction-service 通过 AudioSpeechClient 发送文本，不直接访问 ALSA 或传输 PCM。
- audio.proto 与 voice.proto 分离保留，分别表达真实播放指标和文本请求投递指标。

### 设计决定

- audio-service 独占麦克风和扬声器；voice-interaction-service 只负责对话与动作编排。
- TTS PCM 只存在于 audio-service 进程，跨服务仅传输 response text。
- 播放使用唯一 worker，停止时丢弃待播队列，只允许当前播放收尾。
- mock TTS 是可听测试提示音，不冒充真实语音合成。

### 验证结果

- speech_output_test 覆盖 PCM 格式、后台播放、生命周期和非法依赖。
- CTest 14/14 通过。
- 完整 smoke 验证 Speak -> mock TTS -> ALSA null，queued=1、played=1、failed=0。

## 2026-06-23 - 受控语音动作分发

### 变更内容

- 新增平台无关 ActionDispatcher 和 ActionExecutionResult。
- mock dispatcher 仅接受类型化白名单动作，非法枚举返回 rejected。
- voice response 新增 action status/message，区分意图识别和动作执行结果。
- service status 新增 attempted、succeeded、failed 动作指标，voice-ctl 同步展示。
- 缺少 dispatcher 时返回 not_implemented，不再让响应表现为动作已完成。

### 设计决定

- dispatcher 只接收 VoiceAction，不接收命令字符串，也不提供 shell 执行入口。
- 当前运行时使用 mock dispatcher；真实动作后续通过相机、媒体等用户可感知服务客户端实现。
- response_text 和 action execution 分离，便于后续 TTS 选择可靠的最终播报内容。

### 验证结果

- 测试覆盖白名单成功、none、非法动作拒绝和 dispatcher 缺失路径。
- service 测试覆盖动作状态、成功/失败指标及既有有序 response 历史。
- CTest 13/13 通过；完整 smoke 通过并展示新增动作指标。

## 2026-06-23 - 语音交互服务

### 变更内容

- 新增 voice-interaction-service，订阅 audio-service transcript 并生成有序响应事件。
- 新增确定性 mock assistant，识别车辆状态、相机和音乐等白名单意图。
- 新增 VoiceInteractionControl gRPC status、调试处理和 response stream 接口。
- 新增 voice-ctl status/process/responses 调试工具和 systemd 单元。
- YAML 新增 voice interaction 上游地址、重连参数和 127.0.0.1:50053 监听地址。

### 设计决定

- 意图识别位于平台无关 modules/voice，服务负责生命周期、指标和 gRPC 边界。
- mock action 仅生成类型化动作，不执行 shell 或安全关键车辆控制。
- 默认 features.voice.enabled 为 false；smoke 验证禁用状态，正向意图路径由单元测试覆盖。
- response 历史容量为 32，仅用于短时重连恢复，不承担持久化。

### 验证结果

- mock assistant 测试覆盖全部白名单意图和 unknown fallback。
- service 测试覆盖禁用状态、顺序响应、指标、历史和非法输入。
- CTest 13/13 通过；完整 smoke 包含 voice gRPC 启动、探活和状态查询。

## 2026-06-23 - Mock ASR 与转写事件

### 变更内容

- 新增 `modules/voice`、`SpeechRecognizer`、`SpeechTranscript` 和确定性 mock provider。
- `audio-service` 新增 ASR consumer thread，按顺序消费完成的 SpeechSegment 并排空停止队列。
- transcript 使用单调 ID 和容量 32 的有界历史，支持多个订阅者按 ID 顺序读取。
- `AudioControl.SubscribeTranscripts` 提供 text-only server stream，不传输原始 PCM。
- `audio-probe --transcripts --count N --timeout-ms N` 可观察转写事件。
- status 新增 ASR enabled、processed、published 和 error 指标。
- topic gRPC subscriber 在总 deadline 内自动续连，避免瞬时 UNAVAILABLE 中断 echo/hz。

### 设计决定

- ASR 与 ALSA/VAD 解耦，只依赖完成的 SpeechSegment；未来可替换 whisper.cpp/TensorRT。
- `features.voice.enabled` 控制 ASR worker，默认关闭；当前 provider 仅支持 mock。
- transcript 历史只处理短暂订阅断线，不代替 SQLite 或持久化事件存储。
- 语音段队列只有 ASR 一个 consumer，启用 ASR 后禁止外部抢读。

### 验证结果

- mock recognizer 测试覆盖确定性结果和空段错误。
- ASR pipeline 测试覆盖 segment→recognizer→ordered transcript 与指标。
- CTest 11/11 通过；完整 smoke 通过，默认配置正确显示 ASR disabled。

## 2026-06-22 - 语音段聚合

### 变更内容

- 新增平台无关 `SpeechSegment` 与 `SpeechSegmenter`，将逐帧 VAD 结果聚合为连续 PCM16。
- 支持 100 ms pre-roll、silence endpoint、Stop flush、discontinuity 中断和 15 秒上限。
- `audio-service` 新增容量为 8 的 speech segment SPSC 队列及完成、截断、丢弃指标。
- gRPC status 与 `audio-probe` 增加 segment 数量、截断、队列丢弃和最近时长。
- YAML 新增 `speech_segment.pre_roll_ms/max_segment_ms`，并校验 frame 对齐。
- AudioControl client 启用 wait-for-ready；smoke 中 gateway 改为脚本主动停止，消除有限样本竞态。

### 设计决定

- AudioFrame ring 仍由 VAD worker 独占；下一批 ASR 只消费完成的 SpeechSegment 队列。
- pre-roll 保留 VAD debounce 前的起音，silence hangover 已包含在 segment 尾部。
- 超长语音分段并标记 truncated；采集 discontinuity 立即封段并标记 discontinuous。
- 原始 PCM 与 SpeechSegment 均不经过 gRPC，控制面只暴露指标。

### 验证结果

- `speech_segmenter_test` 覆盖 pre-roll、endpoint、flush、truncation 和 discontinuity。
- `audio_service_test` 验证 segment queue 与 service 指标；CTest 9/9 通过。
- 完整 smoke 通过，null silence 不产生语音段，既有车辆 topic echo/hz 保持通过。

## 2026-06-22 - 本地语音活动检测

### 变更内容

- 新增平台无关 `VoiceActivityDetector` 接口和 dependency-free `EnergyVad`。
- 能量 VAD 输出 RMS dBFS，并通过连续 speech frames 与 silence hangover 稳定状态转换。
- `audio-service` 新增唯一 VAD consumer thread，持续消费 SPSC ring 并统计事件。
- gRPC status 和 `audio-probe --status` 输出 VAD 状态、输入电平及处理指标。
- YAML 新增 VAD backend、阈值和帧窗口，并校验 backend、范围与正整数约束。

### 设计决定

- 当前使用能量 VAD 保证 WSL/Jetson 可直接运行，不提前引入 WebRTC 依赖。
- VAD 遇到 discontinuity 会重置 debounce，避免丢帧前后的音频被拼成一次语音。
- VAD worker 是 ring 的唯一 consumer；后续 ASR 从语音段事件接入，不直接抢读 ring。

### 验证结果

- `energy_vad_test` 覆盖 dBFS、speech debounce、silence hangover 和 discontinuity reset。
- `audio_service_test` 验证 VAD worker 持续消费采集帧；CTest 8/8 通过。
- 完整 smoke 通过，ALSA `null` 被识别为 silence、-120 dBFS，VAD metrics 持续增长。

## 2026-06-22 - C++ 标准基线

- 全工程统一使用 C++17，不提供 C++20/23 构建开关。
- README、架构和配置文档统一使用 C++17 表述。

## 2026-06-22 - 音频采集服务

### 变更内容

- 新增 `audio-service`，统一持有 ALSA capture source 和 `AudioCaptureStream`。
- `AudioControl` gRPC 支持 start、stop、status 和实时 metrics，PCM 不通过 gRPC 传输。
- `audio-probe` 新增 `--start/--stop/--status` 远程控制命令。
- Start RPC 等待设备打开结果；本地 PCM 保留为后续 VAD/ASR 的进程内数据面。
- 新增 service 生命周期单元测试、ALSA `null` gRPC smoke 和 systemd 单元。

### 设计决定

- gRPC 是控制面，本地 SPSC ring 是数据面；唯一 consumer 留给后续真实 VAD/ASR。
- `audio-service` 独占麦克风设备；UI 和语音模块不直接访问 ALSA。
- 当前批次只完成采集控制，扬声器播放控制仍为后续能力。

### 验证结果

- CTest 7/7 通过；完整 smoke 验证 gRPC start/running/status/stop 和既有车辆链路。

## 2026-06-22 - 音频采集流引擎

### 变更内容

- 新增平台无关 `AudioCaptureSource` 接口和单采集线程 `AudioCaptureStream`。
- ALSA capture 改为 non-blocking + poll，返回 ok/timeout/xrun/stopped/device error 状态。
- 新增 `AlsaCaptureSource` 适配器，并严格校验 16 kHz、mono、PCM16、20 ms 语音格式。
- 支持 partial read 拼帧、XRUN 恢复标记、ring overflow 丢帧计数和错误状态查询。
- `audio-probe --capture` 迁移到 poll/status API，不再使用阻塞 bool 读取接口。

### 设计决定

- `modules/audio` 保持平台无关；ALSA poll、设备句柄和 recover 仅位于 `drivers/alsa`。
- capture thread 是唯一 producer，VAD/ASR consumer 不反向阻塞设备读取。
- gRPC 后续只承担 start/stop/status 控制面，原始 PCM 留在进程内数据面。
- stream 停止后可安全清空 ring；故障信息在 `Stop()` 前可查询，停止后状态归一为 stopped。

### 验证结果

- audio、alsa_audio、audio-probe 和新增 stream test 编译通过。
- fake source 覆盖 timeout、XRUN、两个半帧拼接、flags、metrics、故障和停止关闭。
- CTest 6/6 通过；ALSA `null` 经 poll 路径成功采集 16000 帧并写入 PCM16 WAV。

## 2026-06-22 - 实时音频帧与 SPSC

### 变更内容

- 新增不可变 `AudioFrame`，固定为 16 kHz、mono、PCM16、20 ms、320 samples。
- frame 包含 sequence、单调时钟时间戳和 discontinuity/xrun/drop flags。
- 新增固定容量 lock-free `SpscRingBuffer<T, Capacity>`，容量强制为 2 的幂。
- ring 满时拒绝新帧并累计 drop count，不修改 consumer 所有的 read index。
- 新增顺序、回绕、溢出、不可赋值约束和 5 万帧双线程并发测试。

### 设计决定

- 固定语音格式由类型常量表达，不在每帧重复保存 sample rate 和 channels。
- 无效帧不进入数据面；不连续通过 flags 和 metrics 表达。
- 320 个 PCM16 samples 直接按值传递，当前不引入复杂零拷贝内存池。
- ring 仅支持 single producer/single consumer，控制面不得直接访问槽位。

### 验证结果

- audio target 和 `audio_frame_buffer_test` 编译通过，CTest 5/5 通过。
- 5 万帧 producer/consumer 并发测试连续运行 20 次，无乱序或数据损坏。
- 编译期确认当前目标的 index 和 drop metric atomics 为 lock-free。

## 2026-06-22 - ALSA 驱动与音频探针

### 变更内容

- 新增 `drivers/alsa`，封装 PCM 设备枚举、格式协商、读写、xrun recover 和 RAII 关闭。
- 新增 `audio-probe --list/--capture/--play`，复用类型化 audio config 和 WAV 模块。
- ALSA period buffer 配置为约四个周期，并拒绝零进度 I/O，避免死循环。
- 音频 backend 当前严格校验为 ALSA，设备名不能为空。
- 默认 smoke 增加 ALSA 设备枚举。

### 设计决定

- ALSA 细节只存在于 `drivers/alsa`，WAV 和 PCM 领域模型继续留在 `modules/audio`。
- probe 按 period 分块处理并响应 runtime stop，不承担常驻服务职责。
- 真实麦克风/扬声器由 Jetson 实机验证；无硬件环境使用 ALSA `null` 验证软件链路。

### 验证结果

- ALSA 1.2.6.1 driver 和 `audio-probe` 编译通过，CTest 4/4 通过。
- WSL 枚举到 `null` duplex PCM。
- null capture 录制 16000 帧，生成 PCM16 WAV；null playback 完整播放 16000 帧。
- 默认 smoke 包含 audio device list，并保持完整通过。

## 2026-06-22 - 音频核心与 WAV

### 变更内容

- 新增 `modules/audio`，提供 PCM16 格式校验、帧大小和周期帧数计算。
- 新增 RIFF/WAVE PCM16 读写，支持单声道和多声道交错 samples。
- WAV 解析校验格式、完整帧、截断数据和 512 MiB 输入上限。
- Ubuntu 依赖清单增加 `libasound2-dev` 和 `alsa-utils`，为下一批 ALSA 接入准备。
- 新增 `audio_wav_test`，覆盖格式校验、往返、非法文件和缺失文件。

### 设计决定

- `modules/audio` 不依赖 ALSA、Qt 或 gRPC，保持平台无关。
- ALSA 设备句柄放入 `drivers/alsa`，服务生命周期放入 `processes/audio`。
- 当前只支持语音链路需要的 PCM16，小步扩展格式，不预建复杂媒体框架。

### 验证结果

- audio target 和 WAV test 编译通过。
- CTest 4/4 通过。

## 2026-06-22 - Qt UI 数据新鲜度

### 变更内容

- `VehicleStateModel` 新增独立 `fresh` 属性和 1.5 秒 stale timer。
- gRPC 已连接但车辆状态停止更新时，界面显示 STALE，不再把旧值标记为实时数据。
- transport 断开时立即停止 timer 并清除 fresh 状态。
- `ui_model` 拆为独立 target，新增 QtTest 覆盖初始、超时和断线行为。

### 验证结果

- Qt UI 和 `vehicle_state_model_test` 编译通过。
- CTest 3/3 通过，fresh→stale 定时测试耗时约 80 ms。
- QML offscreen 加载无属性绑定错误，SIGTERM 后状态码 0。

## 2026-06-22 - Qt UI 一键联调

### 变更内容

- 新增 `scripts/run-cockpit-ui.sh`，一条命令启动车辆服务、网关和 Qt UI。
- 使用 gRPC topic discovery 探测网关就绪，不依赖固定启动延时。
- UI 退出或脚本收到信号后，统一停止并等待后台服务。
- 默认使用 mock 数据，可通过 `VEHICLE_SOURCE=socketcan` 切换 CAN 数据源。
- 网关上游 gRPC stream 增加停止监视，信号到达后主动取消阻塞式 `Read()`。

### 验证结果

- offscreen 一键链路成功启动并连接 UI、gateway 和 vehicle-data-service。
- 单次 Ctrl+C 后 UI、gateway 和 vehicle-data-service 均完成清理，无残留进程。
- 网关在信号到达后约 20 ms 完成上游取消并退出，不再等待 10 秒 stream deadline。
- Codex 隔离网络中仍观察到 localhost gRPC reset；无代理对照未改善，需在普通 WSL
  终端继续确认宿主环境表现。

## 2026-06-22 - Qt UI 信号退出

### 变更内容

- `cockpit-ui` 增加 Qt event loop 与 `ProcessRuntime::ShouldStop()` 的生命周期桥接。
- SIGINT/SIGTERM 到达后由主线程调用 `QCoreApplication::quit()`。
- `aboutToQuit` 继续负责取消 gRPC context 并 join worker thread。

### 验证结果

- Qt6 QML 页面已在 offscreen 平台成功加载。
- `cockpit-ui` 重新构建通过；单次 SIGTERM 后打印 stopped 并以状态码 0 退出。

## 2026-06-21 - Qt 6 车机界面基线

### 变更内容

- `cockpit-ui` 从 Qt 5.15 构建声明迁移到 Qt 6。
- QML import 移除 `2.15/1.15` 版本锁，使用 Qt 6 模块解析。
- Ubuntu 依赖替换为 Qt 6 base、declarative 和对应 QML runtime modules。
- UI 通过 C++ `GatewayClient` 订阅 CockpitEvent，通过 `VehicleStateModel` 更新 QML。

### 设计决定

- 新项目不保留 Qt5 fallback，减少双版本兼容成本。
- Ubuntu 22.04 的 Qt 6.2.4 只作为 WSL 编译下限。
- Jetson 发布环境固定 Qt 6.8 LTS 工具链，并通过 `CMAKE_PREFIX_PATH` 显式选择。
- Qt UI 保持可选 target，headless 服务构建不依赖图形栈。

### 验证结果

- Ubuntu 22.04 软件源确认可提供 Qt 6.2.4 base、declarative 和 QML runtime modules。
- Qt 6.2 使用通用 `add_executable`、`AUTOMOC` 和 `AUTORCC`，不依赖较新 Qt 的
  `qt_standard_project_setup`。
- QML runtime 依赖补充 `QtQuick.Templates` 与 `QtQuick.Window` 模块。
- headless 默认构建和 CTest 2/2 通过，`BUILD_COCKPIT_UI=ON` 编译链接通过。
- offscreen 启动已进入 QML engine；补装新增的 Templates/Window runtime 后继续验证页面加载。

## 2026-06-21 - gRPC topic 发现

### 变更内容

- Gateway 协议新增 `TopicMetadata`、`ListTopics` 和 `GetTopicInfo`。
- 网关公开 `/vehicle/state` 的消息类型、来源和读写能力。
- `topic list/info --backend grpc` 可查询运行中网关，不再依赖文件 registry。
- 默认 smoke 新增 gRPC topic 发现和类型信息验证。

### 设计决定

- 当前元数据由网关显式维护，不提前引入动态注册中心。
- `/vehicle/state` 标记为可订阅、不可发布，避免调试工具伪造车辆状态。
- gRPC 连接和错误映射集中在 `TopicGrpcDiscovery`，命令文件只处理展示。

### 验证结果

- proto 重新生成、增量构建和 CTest 2/2 通过。
- 完整 smoke 中 `topic list` 返回 `/vehicle/state`，`topic info` 返回类型、来源和读写能力。
- discovery、echo、hz 以及上游断线自动重连链路均验证通过。

## 2026-06-21 - 网关事件流与 topic gRPC

### 变更内容

- `cockpit-gateway-service` 实现 `SubscribeCockpitEvents` server-streaming 服务。
- VehicleState 上游消息转换为 CockpitEvent，并以“最新值 + 版本号”方式转发。
- `topic echo` 和 `topic hz` 新增共享 gRPC 订阅后端，当前支持 `/vehicle/state`。
- 新增 `--backend grpc`、`--count` 和 `--max-hz` 参数。
- 默认 smoke 覆盖 vehicle → gateway → topic 的双段 gRPC 数据流。

### 设计决定

- 文件后端继续保留，用于离线调试；gRPC 后端用于运行中服务订阅。
- 网关和 topic 使用生成的 protobuf 类型，不增加无类型内部消息总线。
- 当前只公开已有 schema 的 `/vehicle/state`，未知 topic 明确返回错误。
- gRPC 服务和客户端使用显式 Start/Stream/Shutdown 生命周期，不隐藏后台线程。

### 验证结果

- 增量构建通过，CTest 2/2 通过。
- `topic echo` 通过网关连续收到 3 条 VehicleState CockpitEvent。
- `topic hz` 通过网关测得约 4.963 Hz，与 200 ms mock 发布周期一致。
- 完整 CAN/stdout、双段 gRPC、cloud placeholder 和文件 topic smoke 通过。

## 2026-06-21 - zcarcloud 配置架构参考

### 变更内容

- 审计 `z-car-cloud.yaml`、`CarCloud::InitEnv`、`ConfigManager`、listener runtime 和各 target 依赖。
- 新增 `docs/配置说明.md`，确定当前项目的配置分区、类型归属和启动校验规则。
- Ubuntu 依赖增加 `libyaml-cpp-dev`，用于替换当前两层标量解析器。
- 新增不可变 `SystemConfig` 与 system、paths、logging、services、hardware、features、tools
  类型化子配置。
- `config.yaml` 按职责重新分区，全部服务和工具移除 dotted string key 读取。
- 日志 `mirror_stderr`、gRPC stream timeout/retry 参数开始实际生效。
- 删除重复且未使用的 `configs/logging.yaml`。

### 设计决定

- 采用 zcarcloud 的配置驱动、类型化转换和显式组件生命周期。
- 不复制全局 ConfigManager 单例、Zoo service locator 或 zelos 平台专用依赖。
- gRPC、CAN、MQTT 等组件读取类型化子配置，不再长期保留 dotted string key。

### 验证结果

- yaml-cpp 0.7.0 配置库编译通过。
- `system_config_test` 验证真实配置加载和非法 gRPC 地址路径提示。
- CTest 2/2 通过，完整 gRPC/CAN/topic smoke 通过。

## 2026-06-21 - VehicleState gRPC 数据流

### 变更内容

- 新增 `proto/CMakeLists.txt`，通过 `protoc` 和 `grpc_cpp_plugin` 生成 `contracts` target。
- proto package 统一为 `cockpit.proto.*`，避免生成类型与领域模型同名冲突。
- `vehicle-data-service` 新增同步 server-streaming 服务，将最新 VehicleState 推送给订阅者。
- `cockpit-gateway-service` 新增 streaming 客户端、10 秒限时重连和时间戳去重。
- 默认 smoke 改为双进程 gRPC 集成测试，自动启动和停止 vehicle 服务。

### 设计决定

- 生成代码只进入 `build/proto/generated`，不提交到仓库。
- 服务端只保存最新状态和版本号，不无限缓存过期车辆状态。
- 本机 gRPC 使用 insecure credentials；远程接口不得直接复用该安全配置。
- Vehicle gRPC 默认只监听 `127.0.0.1:50050`，不暴露到 Jetson 外部网卡。
- 本机 channel 显式关闭 HTTP proxy，避免系统代理干扰 localhost 服务通信。

### 验证结果

- protobuf 3.12.4、gRPC 1.30.2 代码生成和编译通过。
- `cockpit_smoke_test` 通过。
- gateway 通过 `127.0.0.1:50050` 连续收到 3 条 VehicleState，完整 smoke 通过。

## 2026-06-21 - gRPC 构建前置

### 变更内容

- 检查 WSL2 当前工具链，确认尚未安装 `protoc` 和 gRPC C++ 开发库。
- 确认 Ubuntu 22.04 apt 提供 protobuf 3.12.4 与 gRPC 1.30.2。
- `scripts/install-dependencies.sh` 增加 protobuf 编译器、gRPC 插件和 C++ 开发包。

### 设计决定

- 当前阶段使用 Ubuntu 系统包，暂不引入 Conan、vcpkg 或源码编译 gRPC。
- 依赖安装完成后再提交并验证 proto 生成和 gRPC 业务代码，避免未编译代码进入主链路。

### 验证结果

- 依赖已在 WSL2 安装，`protoc`、`grpc_cpp_plugin` 和 pkg-config 检查通过。

## 2026-06-21 - SocketCAN 车辆数据链路

### 变更内容

- 新增平台无关 `VehicleCanCodec`，定义可测试的原型 `0x123` VehicleState 帧。
- `vehicle-data-service` 支持 `--source mock|socketcan`，并将业务循环从 `main.cc` 拆出。
- SocketCAN 接收支持 poll 超时、空闲退出、无关帧忽略和非法帧告警。
- `can-simulator` 改为通过相同 codec 生成车辆状态帧。
- 新增 `scripts/setup-vcan.sh` 与 `scripts/tests/vcan-smoke.sh`。
- Ubuntu 依赖增加 `can-utils`、`iproute2` 和 `kmod`。

### 设计决定

- CAN 字节映射属于 `modules/vehicle`，Linux socket 访问继续留在 `drivers/socketcan`。
- `0x123` 是 WSL/Jetson 原型测试协议，不冒充旧项目或真实车辆 DBC。
- 正式 DBC 到位后替换 codec，不改变 SocketCAN 驱动和服务生命周期。

### 验证结果

- 标准 `build/` 增量构建完成，`cockpit_smoke_test` 通过。
- codec 往返、无关帧忽略和短帧拒绝均有测试覆盖。
- 默认 mock/stdout `scripts/tests/smoke.sh` 完整通过。
- 在 WSL2 交互终端运行 `scripts/tests/vcan-smoke.sh`，成功创建并启用 `vcan0`。
- `can-simulator` 通过 SocketCAN 发送 3 帧，`vehicle-data-service` 完整接收并解码为
  12.0、15.5、19.0 km/h，对应挡位与 SOC 也一致。

## 2026-06-20 - common 职责复核

### 变更内容

- 抽样检查 `znavigator`、`safe_ota`、`zcarcloud` 以及云端前后端工程的分层方式。
- 确认 zelos 中的 `common` 主要保存组件内部共享的状态、协议结构和抽象接口，
  并不是跨项目基础设施的固定目录。
- 当前仓库不恢复顶层 `common`；对应职责由 `proto` 和具体 `modules` 承担。
- 删除兼容用 `core` 聚合 target，服务和工具改为声明最小直接依赖。
- 修正旧项目审计中被误改的 `zcarcloud/common` 原始路径。

### 设计决定

- `core/` 只是基础设施分类目录，不再代表“一次链接全部基础库”的 target。
- 只有出现被多个同级模块共享、且无法归属现有领域的接口或状态类型时，才考虑
  `modules/common`；禁止用它收纳暂时不知道放哪里的代码。

### 验证结果

- 标准 `build/` 完成 38/38 构建步骤。
- `cockpit_smoke_test` 通过。
- 完整 `scripts/tests/smoke.sh` 链路通过。

## 2026-06-20 - 核心、模块与驱动分层

### 变更内容

- 将通用基础设施从 `common` 迁移到 `core`。
- 将 `vehicle` 和平台无关的 `CanFrame` 迁移到 `modules`。
- 将 Linux `SocketCan` 迁移到 `drivers/socketcan`，并新增独立 `socketcan` target。
- protobuf 契约迁移到顶层 `proto`，生成代码继续放在 `build` 目录。
- 顶层 CMake 仅聚合 `core`、`modules`、`drivers` 和产品目录。

### 设计决定

- `core` 只提供配置、日志、生命周期和基础工具，不包含领域模型或硬件访问。
- `modules` 保存平台无关的领域能力；`drivers` 保存 Linux 与硬件适配。
- `socketcan` 依赖 `can`，反向依赖禁止；服务和工具按需链接最小 target。
- 内核模块和设备树可放在对应 `drivers/<device>` 下，但不进入默认用户态构建。

### 验证结果

- `core`、`modules`、`drivers` 分层在标准 `build/` 中配置和构建成功。
- 独立生成 `libcan.a` 和 `libsocketcan.a`，依赖传播正确。
- `cockpit_smoke_test` 与完整 smoke 链路通过。

## 2026-06-20 - common 构建模块化

> 本节记录迁移前的首次模块化；当前目录边界以上一节为准。

### 变更内容

- 参考 `znavigator`，当时为配置、日志、生命周期、工具、车辆与 CAN 模块分别增加
  独立 `CMakeLists.txt`。
- 当时由统一聚合入口管理各子目录；该入口现已拆成 `core`、`modules` 和 `drivers`。
- 每个模块声明自己的直接依赖。
- `core` 改为 INTERFACE 兼容聚合 target，现有服务暂时无需整体改链接方式。

### 设计决定

- 新代码优先链接实际需要的最小 target。
- 不提前创建没有源码的 `audio`、`ai` 空 target。
- `core` 不聚合 `can`、`audio` 或 `ai` 等功能模块。

### 验证结果

- 标准 `build/` 目录重新配置并完成 37/37 构建步骤。
- 成功生成 `libconfig.a`、`liblogging.a`、`libruntime.a`、`libutils.a`、
  `libvehicle.a` 和 `libcan.a`。
- `cockpit_smoke_test` 通过。
- 完整 `scripts/tests/smoke.sh` 链路通过。

## 2026-06-20 - 文档收拢

### 变更内容

- 根 `README.md` 更新为当前仓库、已实现能力和可运行命令入口。
- `docs/文档导航.md` 调整为文档导航和文档优先级说明。
- 纳入完整架构蓝图 `docs/architecture_refined_v0.3.md`。
- 架构蓝图中的项目名、安装路径和当前阶段说明统一为 cockpit-system。
- 项目名、仓库名和部署目录统一使用 `cockpit-system`。
- 明确当前只维护 `cockpit-system` 主仓库，云端前后端是未来可选拆分。
- 修正当前架构快照的蓝图链接，并统一 scope/modularization 中的仓库名和 CAN 状态。

### 验证结果

- 文档链接和旧项目名通过文本检查。
- 清理旧 CMake cache 后，使用标准 `bash scripts/build.sh` 重新配置并构建成功。
- `cockpit_smoke_test` 通过。

## 2026-06-20 - SocketCAN 基础层

### 变更内容

- 新增内部 CMake target：`can`。
- 新增 `CanFrame`，支持标准帧/扩展帧 ID、DLC、标志位校验和 candump 格式输出。
- 新增 `SocketCan`，支持 fd RAII、移动语义、接口绑定、发送、poll 超时和接收。
- `can-simulator` 新增 `--backend stdout|socketcan`。
- 当时新增 `can.simulator_backend` 配置（现为 `hardware.can.simulator_backend`），安全默认值为 `stdout`。
- `cockpit_smoke_test` 增加 CAN 帧测试。
- 参考并审计 `/home/ffz/code/project/无人车/can/can_ws/src/can_analyze`。
- 变更记录调整为中英双语格式。

### 设计决定

- Linux/SocketCAN 细节封装在 `SocketCan` 内部。
- 不复制 ROS2 依赖、全局 socket、分离线程和固定全局帧缓冲区。
- 没有 `vcan0` 或真实 CAN 硬件时，`stdout` 模式仍必须可运行。

### 验证结果

- 使用 GCC 11.4.0 和 Ninja 在标准 `build/` 目录中构建成功。
- `cockpit_smoke_test` 通过。
- 默认 `stdout` CAN 后端下，完整 `scripts/tests/smoke.sh` 链路通过。
- 本节记录时尚未配置 `vcan0`；该验证已于 2026-06-21 完成，见最新记录。
